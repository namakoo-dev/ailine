# 需要⑤ 担当者別に分けて配る ── 検体と採点（2026-09-12）

    python bench/split_people/mk_split.py                 # 6 冊と 答え_split.json（生成物は gitignore・LO 不要）
    python bench/split_people/score_split.py --self-test  # 対照 3 本（答えをそのまま／黙る／全員 1 冊に）

検体は**規則を知らない側**が書いた（担当者列の見出しのゆれ・空欄・表記ゆれ・複数担当・合計行・非表示シート・
「担当」と「営業担当」が同居して決められない冊・陰性対照）。答えは 1 冊ごと・行番号で。
見出しにする数は ★誤配／★混入／漏れ／証明の嘘／偽の疑い／不親切／分けた（分けるべきでない）／正 ── 正答率にしない。
採点器は `ailine_core` を import しない。設計は `docs/DESIGN-20260912-担当者別に分けて配る.md`。
