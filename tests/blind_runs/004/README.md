# 004 ── 盲検 4 体目（2026-09-18・記帳代行）

役: 記帳代行の事務所の担当者。冊: 顧問先の請求書 5 通 ＋ 9月仕訳（科目が空欄）＋ 過去の仕訳。
条件は 1〜3 体目と同じ（★ 直した箇所・こちらの心配・過去の結果・値段の前例は渡していない）。
GUI は禁止（Namakoo の決定で別途本人が確認する）。

## ★★ この回は分母が**欠けている**

`trace.jsonl` が**無い**。`AILINE_TRACE` を買い手役のシェルに渡し損ねたため
（こちらのシェルで export しても別プロセスには継承されない・2026-09-18 の取りこぼし）。

- 在るのは `history.jsonl`（11 件）だけ ── **run 系しか残らない**。
  `doctor` / `ops` / `forms` / `accounts` / `split` / `verify` は 1 行も入っていない。
- つまりこの回は **`replay` / `floor` / `sheet` に掛けられない**。凍らせてあるのは
  「初期の冊」と「run 系の履歴」まで。
- ★ 打った全コマンドは**買い手の報告文**にしか無い（docs/DESIGN-20260918-買い手役4体目で出た穴.md）。

★ 次の回（005）は、買い手役に渡す環境そのものに `AILINE_TRACE` と `AILINE_HOME` を
  設定してから出すこと。**渡したつもりで渡っていない**のが今回の取りこぼし。

## もう 1 つの汚染

入っていた `ailine` が **v0.1.0（古い版）**だった。こちらが版を確かめずに渡したため、
買い手は「README に在るコマンドが無い」で 30 分溶かしている。
★ ただしそこから本物が出た ──「`doctor` がバージョンを一言も言わない」。
