# Changelog

## [v0.2.5](https://github.com/namakoo-dev/ailine/compare/v0.2.4...v0.2.5) - 2026-09-10

**★ この版は道具の挙動を 1 つも変えていません。**（`src/` の変更 **0 ファイル**）

★ commit 数・ファイル数・行数は**書きません**。書いた瞬間に、その commit 自身でずれるからです
（v0.2.4 で「出した後に数え直して直す」を、v0.2.5 で「直したらまた増える」を実際に踏みました）。
数えたい方は次で出せます:

```
git log --oneline v0.2.4..v0.2.5 --no-merges | wc -l
git diff --shortstat v0.2.4..v0.2.5
```
変わったのは**次に作るものの設計と、その根拠になる検体**です。
使い勝手や精度を期待して上げると、何も変わりません。上げなくても支障ありません。

### なぜ文書だけの版を切るか

この道具の売りは「判断の跡が追えること」です。今回残したのは**外れた判断の記録**で、
成果より価値があると考えました ── 同じ穴を次に踏まないための地図です。

---

## 何をしていたか

次に作るのは「**N 冊の請求書から、決まった項目を集める**」機能です。
需要の記録では、表計算で手作業だと 20 秒で終わらない作業の上位 5 件のうち **4 件が複数ファイル**で、
いまの ailine の語彙はその 22 項目を **1 件も覆っていません**。

作る前に測りました。**そして測るたびに、前提が崩れました。**

### 崩れた前提（この版が記録している本体）

```
① 合成した検体の構造が想像だった
     実物の請求書は「表」ではなく**帳票**。取引先は列でなくセルに在り、
     明細の和は小計（税抜）であって請求額ではない。
     ★ 合成に合わせて作ると、全件が消費税ぶん少ない数字を静かに出すところだった
② 実物で答えを持たずに「取れた」を数えた
     雛形は伏せ字（〇〇 〇〇）なので、取れたは数えられても**正しいが言えない**。
     ★ 宛先を 16 冊中 15 冊で誤って取り、それを「15/15 成功」と報告した
③ 検体の答えを、成果物でなく自分の想定から作った
     雛形に見本の明細が入っていることを見落とし、40 冊すべてで外した
④ 検体で満点だった規則が、実物で崩れた
     「レイアウトごとにセル番地を固定する」は検体で 40/40。だが実物 22 冊は **14 種類**の
     レイアウトに割れ、1 種類 1 冊が 10 種類。番地の固定は成立しない
⑤ ★ 自分の採点器が恒真だった
     「同じ金額が独立した 2 つ以上の読み口から出たら確か」という検算を作り、40/40 を得た。
     だが**結合セルの写しを 2 口目に数えていた**。出所で畳むと 2 口=14 冊 / 1 口=26 冊。
     ★ 26 冊は**自分の写しと一致して「裏が取れた」**と言っていた
```

★ ⑤がいちばん痛い。この道具は買い手の条件として「**検算が本体と同じ口から出るなら、
それは検算でなく感想**」を掲げているのに、その検算を測る採点器が、まさにそれをやっていた。

### どうやって見つけたか

**設計を、材料だけ渡して 3 人に独立に書かせました**（互いの答えは見せない）。
1 巡目で 3 人が同じ結論に着き、その結論を測ったら 3 つ崩れました。
2 巡目は「測定は決着済み・再議論するな」と渡したのに、**3 人ともその上で測り直して
6 つ覆してきました**。全部こちら側の誤りでした。

★ 1 人に聞いていたら「そういう見方もある」で終わっていたと思います。

---

## 置いたもの

```
docs/DESIGN-20260910-…   設計と、崩れた前提の記録（§0b〜§0e）。★ 設計本体はまだ書いていない
bench/received_invoices/  答え付きの検体を作る仕掛け（骨は実物の雛形・値だけ流し込む）
                          ★ README の冒頭に「この検体の既知の欠陥 4 つ」を書いてあります
bench/folder_map/         最初の合成検体（①で構造が想像だったと分かったもの）
```

★ 雛形そのものは他社の著作物なので repo に含めていません（各自で入手して隣に置く形）。

### ★ 検体を、規則を書く者とは別の者に書かせた（87 冊）

崩れた前提の ①〜⑤ は、どれも「**自分で検体を作り、自分で規則を作った**」ことから出ました。
そこで検体づくりを切り離し、**どんな規則を作るつもりかを伝えずに**依頼しました。

```
群      基礎 36 ／ 税率 6 ／ 敵対 40 ／ 混入 5      骨 12 種（4 ベンダの雛形）
① 交絡  骨 6 種 × 明細行数 {0,1,2,3,5,8} の総当たり・各セル 1 冊
② 宛先  「御中のセル == 宛先社名」が 12/36 でしか真でない（前の検体は作り方で全部真だった）
③ 敵対  40 冊。うち**陰性対照 6 冊**（正しい冊に誤って印を立てる＝誤報も測れる）
④ 軽減税率  6 冊。骨によって持ち方が 3 通り（記号列 / 税率列 / 単一税率セル）
```

受け入れは 2 段にしてあります ──「生成器が書く前に値を予測して突き合わせる」だけでは
**予測が全部当たったまま罠だけ抜けている**を捕まえられないので、
答えも骨の番地表も見ずに成果物のセルを走査する**別実装の点検**を並べています。
（受け入れ 87/87 ／ 罠の点検 54/54）

### 実装より先に置いた番人

```
区分（確/単/割/無）を作れるのは 1 モジュールだけ ── 出口ごとに文字列を組み立てると
『片方だけ直る』事故の足場になるため。★ 変異 5/5（陰性 3 で赤・陽性 2 で緑）で確かめた
```

### 凍結した判断（実装の前に決めた）

```
根拠が食い違ったら **値を出さない（空欄）**。両側の数字は検分シートに出す
★ 空欄には必ず理由を添える ── 「取れなかった」と「矛盾していた」を同じ顔にしない
★ 理由が『適切か』も機械で確かめる ── 番地が実在するか・値が実際のセルと一致するかを
  **別の口から読み戻す**。「見つかりませんでした」を全件に書けば通る形にしない
```

## 直していない既知の穴（v0.2.4 から変わらず）

```
① 複数条件（AND）を断らずに通す
② グラフの項目列が値列より右にあると、系列名・項目名・値が入れ替わる（真因は特定済み）
③ 転記のキー列に、モデルが対象列と同じ列を入れる回がある
★ ②③は ✓ を出しません（道具が自分で見つけて × を出し、原本には被せません）
```

## 実機テストの記録

文書だけの commit を押そうとして、pre-push の実機テストが 2 回落ちました。
コードは 1 行も触っていないので台を疑ったところ、**LibreOffice が 252 個**残っていました。
掃除したら 78 件すべて緑。テストの後始末は在ったのですが、
「接続先だけを落とす」という安全側の設計が、繋がっていない待ち受けを取りこぼしていました。

★ 4 日前（2026-09-06）に「居残りが失敗の原因だという証拠は無い（再現できなかった）」と
**断定せずに書き残していた**ので、今回の実弾がそこに繋がりました。
断定していたら、その記録は書いた時点で間違いでした。

★ 直しはまだ入れていません（版を出した直後に試験の後始末を変えると、
**変えた後始末を誰も試していない状態で次の版を迎える**ため）。発火条件つきで保留にしています。


## [v0.2.4](https://github.com/namakoo-dev/ailine/compare/v0.2.3...v0.2.4) - 2026-09-09

**外の目が挙げた `✓` の嘘を塞ぎ続け、「語彙の共食い」を測る器を据えた**版。
31 commit / 232 ファイル / +6,616 行（★ 版を出した後に数え直した。書いた時点では
23 / 228 / +5,952 で、出荷直前に足した 4 commit ぶんずれていた）。

前の版で塞いだのは「依頼が痩せた分は事後条件から原理的に見えない」という 1 つの形だった。
この版で分かったのは、その穴が**もう 1 段上**にもあることだ ── **op を足すと、意味の近い
既存の op から静かに仕事を奪う**。奪われた側は「壊れた」と鳴らない。別の正しい仕事を
正しくやって `✓` を出す。

### ① 外の盲検が挙げた false ✓ は、挙がった分を塞いだ（★ 打ち止めではない）

★★ **先に限界を書く。この節は「もう false ✓ は出ない」を意味しない。**
塞いだのは**その回に見えた形**であって、種類ではない。判定はどれも名簿と語彙
（「取り除く」系の語・裸の動詞・扱えない書式の名前）で書いてあるので、**名簿に無い
言い方は素通りする**（`docs/開発手法.md` §6d「見た検体で作った規則は、見た検体でしか
効かない」）。外の目が拾った欠陥を塞いだ commit は**この版だけで 6 本**あり、
当てるたびに前の回に無かった形が出ている:

```
git log --grep=盲検 --grep=検品 --grep=査定 v0.2.3..v0.2.4
```

買う側の査定役は「受け取った `✓` 8 件のうち **3 件（37.5%）** が、頼んだことと違うことを
して `✓` を出した」と書き、**その 3 件が 0 になったら ¥15,000** という条件を出した。
3 件とも潰した。初見の利用者役の所見 5 件も、この手で再現してから直した。

★ いちばん多かった形は「**破壊を頼まれて、破壊しなかったのに `✓`**」だった:

```
「ヤマノ食品の行を削除して」→ 操作:抽出（新シートを作り、元の 6 行はそのまま）→ ✓
「田中さんの分だけ残して他は消して」→ 抽出（元は無傷）→ ✓
```

宣言と実体は一致している。落ちているのは**依頼**だけなので、事後条件は通る。
判定を **op 名でなく「効果の種類」**に替えた ── 依頼が「取り除く」系の語彙に当たり、
実行した op が remove を書かないなら `✓` を出さない。

| 見方 | 実測 | 誤爆 |
|---|---|---|
| op 名で比べる | COMPUTE_COLUMN ← 「列を追加」 が 22 件 | ★ 多い |
| **効果の種類**で比べる | 走行 4,538 件中 **16 件（0.35%）** | **0**（全部が本物の欠陥） |

照合語彙が「**行を**消して」しか持たず「他は消して」に当たらなかったので、**裸の動詞**
（消して／削除して／取り除いて…）まで見るよう広げた。★ 広げる前に測っている ──
実走行 4,556 件で 0.37% → 0.70%、増えた 15 件はすべて既に本物と数えた家系、誤爆 0。

★ **この合格は自分では名乗れない。** 相手の 20 本で 0 件になったが、私はその 20 本の
中身を見てしまっている。だから里程標の合格条件を「**新しい盲検でも false ✓ = 0**」と
定義し直した（`docs/開発手法.md` §6d）。

★★ **その定義で測ると、里程標 A はこの版でも未達だ。** 定義し直した直後に当てた盲検 B が、
新しい false ✓ を出した ── 「合計より上の行だけ抽出して」に対し、条件を誤変換して **0 行**を
抽出し、事後条件は「0 行が一致 → 0 行を抽出（値・型とも保存）」と**自分で数えて** `✓` を
出した。実物は見出しだけの空シートで、抽出としては嘘をついていないが依頼は満たしていない
（`空の成果物に ✓ を出さない` の回）。**塞いだ数ではなく、当てるたびに出るという事実の方が
この版の所見だ。**

### ② 語彙の共食い ── 足した op が、近い op から奪う

出所は Namakoo の問い ──「語彙が増えるにつれて類似の意味に引っ張られるケースは
増える。制御可能か」。列移動を足した直後、効果の行列が **241 → 233/235** に落ちた。
揺れではなく退行で、原因は 2 つとも同じ家系だった。

★ **直しは語彙でなく実表**。「鈴木の右に東棟」が『列移動』へ流れたのは、読み直しの門が
reorder を**無条件に**「別の仕事」と数えていたから。行き先が実在の列に接地しないなら
列の話ではないので、その計画には門を閉じる資格が無い ── `op` 名の `if` ではなく宣言
（`needs_col_anchor`）で書いた。op が増えても配線が要らない形に保つため。

★ **op を足すことより、説明を厚くすることの方が高くつく。** 実物の CLI で説明 0/1/2 行を
A/B した（各 4 回）:

```
0 行   列追加 4/4 ・ 列追加 4/4 ・（列移動は届かない＝正しい）
1 行   列追加 4/4 ・ 列追加 4/4 ・ 列移動 4/4          ← 全部正しい
2 行   ★ 行挿入＋中央揃え 4/4 ・ 列追加 3/4 ・ 列移動 4/4
```

壊れ方は「列移動に化けた」ではなく**無関係な雑音**だった ── 意味で引っ張られたのでなく、
2 行足した分だけ既存の op が押しのけられた。**量**の問題。説明を 1 行に削った。

★ 最初の A/B は模型（単一シートの `book_meta` を手で組んだ）で、**逆の答え**を出していた。
実物の 2 シートで測り直したら答えが変わった。以後この種の A/B は実 CLI でしかやらない。

### ③ いちばん広い op が、いちばん薄い分母だった

②で据えた奪い合いの観測器（`bench/vocab_reach.py`）を、広い op まで厚くした最初の一撃で
これが出た:

```
依頼    「みかんの下に「ぶどう」の行を足して」
読み直し「『一括書換』でなく『1セル書換』として読み直しました」
実物    ★ みかんの商品名が『ぶどう』に上書きされる（行は増えず、みかんが消える）
```

「下に」も「足して」も見ていない。**データが 1 つ消える**側の実害で、この版で塞いだ
どれより重い。

★ なぜ 13 分の効果の行列で一度も出なかったか ── `ADD_ROW` は走行の **18.1%＝いちばん広い
op** なのに、器の分母に**入っていなかった**。器は「行列が覆っていない op だけを見る」目的で
作られ、カバー済みの op を意図的に外していた。数え直したら、器が覆っていない op が走行の
**61.8%** を占めていた。上位 8 op を足して 54 → 78 件にした。

### ④ 語彙の外は、近い op で代わりにやらない

「ウィンドウ枠を固定して」のように**語彙に無い**依頼が、近い op に吸われて `✓` が出る形を、
**凍結した 20 検体の上では** 0 にした（単発 15 / 複合 5）。★ 20 検体は私が選んだので、
①と同じ限界がかかる ── 名簿に無い言い方は素通りする:

```
              A 断り   B 語彙外段   C 吸われ   ★ D 吸われて ✓
着手前          17        0            2            1
やらないこと名簿 18        1            1            0
機能まで見る    20(100%)   0            0            0
```

★ 名簿に足す前に、人が「別の名前で出来ないか」を問うて落としたものがある（理由も並記した）:
フィルタ → 『抽出』で出来る／グループ化 → 『集計』で出来る／条件つき書式 → 『条件つき書換』と
紛らわしい。★ この番人の限界も明記した ── **別名で持っている能力は見えない。**

あわせて、語彙外の段に渡すヘルパを実装本文でなく**呼び方（署名＋引数の意味）**だけにした
（26,547 → 4,510 tok）。窓は 32768 から **8192 へ戻した** ── Namakoo 決裁「ノート PC 想定で
ハードウェア性能への依存を避けたい」。窓で解決すると、保証が買い手の VRAM 次第になる。
★ 正直な限界: この段には事後条件チェッカーが無く、`ok=True` は「動いて変わった」までしか
意味しない。画面には「機械保証なし」と出る。**絞っても悪くならない**ことだけが言える。

### ⑤ 依頼に**無い**値が実体に現れる向き（★ 出荷直前に見つけた 2 件）

この版を出す直前、実機テストが落ちたのを辿って**逆向きの穴**が 2 つ出た。①〜④で塞いだ
のは「依頼に在るものが宣言から落ちる」向きで、こちらは誰も見ていなかった。

**(a) 頼んでいない値を書いて `✓`（実測 11 回中 7 回）**

```
依頼   「5行目に丸山工業の行を作って」   ← 値の指定はどこにも無い
実物   5行目 = ['丸山工業', 1, 1000, 1000]
画面   ✓ 機械検証済み
```

★ 重いのは 2 次被害の方だった ── 発明した値は式の継承の除外集合に入るので、
**その列の式が入らなくなる**。式を持つ表で 6 回実測:

```
捏造なし   金額 = '=B5*C5'   式が継承された
捏造あり   金額 = 1000       ★ 式が入らない（件数を直しても追随しない）
```

直しは「依頼文に接地しない値を落とす」1 箇所（判定は `ailine_core/intent.py`）。落とすと
その列が「指定なし」に戻り、**既存の式の継承が発火する**。捏造 6/6 → 0、式 1/6 → 6/6。
★ 落とした列は解釈行に名指しで出す（黙って空にするのは別の嘘になる）。
★ 数は**数として**比べる ── 部分文字列だと `件数=1` が依頼文の `1000` に当たる（自分で踏んだ）。

**(b) 依頼の主語が落ちて、警告も無く `✓`（実測 4/4）**

```
依頼   「5行目に丸山工業の行を作って 件数3 単価1500」
実物   5行目 = [None, 3, 1500, '=B5*C5']     ← 丸山工業が入っていない
画面   ✓（⚠ すら出ない）
```

「丸山工業の行」に丸山工業が無い。請求書なら取引先名の無い行だ。残差ゲートは**列名**しか
見ないので、値である『丸山工業』は端から対象外（設計どおりの限界）。

★ 直しは**依頼文を 1 文字も読まない構造判定**にした ── 見るのは「行を作ったか」と
「1 列目が空か」の 2 つだけ。読む側に寄せると「値の残差ゲート」になり、②で測った
**語彙を広げると近い op から奪う**危険地帯に入る。4/4 → 0/4、誤爆は 6/6 で 0。
★ 落ちた値を機械が入れ直すことはできない（モデルの出力にも生成された Basic にも
残っていない）。**落ちたことを人に伝えるだけ**。拾えるのはキー列だけで、3 列目だけが
落ちた回は見えない。

★ ゴールデン 697 件で **ADD_ROW 以外の出力は 1 件も動いていない**（②の共食いが
判定を足した側でも起きていないことの確認）。

### ⑥ 位置と語の読み（4 件）

```
表の端        「一番下」「最後」は位置であって、行の名前ではない
行の追加      「〜の行の下に…追加して」が通り、しかも下に入る
除いて        「除いて」は対象から外す意味であって、削除ではない
食い違い      行番号と値が食い違ったら ✓ を出さない／聞き返す前に実表へ聞く
```

### ⑦ 隠さずに開示したもの

- README の看板デモの写真③が実体と食い違っていた（古い挙動のまま）。**開示して直し、
  実機で走らせて突き合わせる番人を置いた** ── 山場が手書きの約束だったから腐った。
- 「決めたこと・保留していること」に索引と番人を付けた（`docs/DECIDED-*` `docs/PENDING-*`）。
- ★ **いま分かっている穴**（3 件・どれも塞がずに出す）:

```
① 複数条件（AND）  語彙に無いので断るはずが「所属が営業かつ内線が200以上の行を
                    抜き出して」は通ってしまう（行列の別枠 1 件）
② グラフの項目列    項目名の列が値の列より**右**にあると、系列名・項目名・値の
                    3 つとも入れ替わる。宣言（解釈行）は正しく、実体だけがずれる
③ 転記のキー列      モデルがキー列に対象列と同じ列を入れる回がある
                    （空の列を空の列で引くので 1 件も一致しない）
```

★ ②③は **`✓` を出していない** ── 道具は間違ったものを作り、自分の事後条件で見つけて
`×` を出し、原本には被せない。①②のような「黙って `✓`」とは段が違う。
★ ②は**真因まで特定した**（`addNewByName` は役割を配列の順序でなく**物理的な左右**で
決める。同じ依頼文で列の並びだけ変えた 2 検体で実証）。直しは次の版
（`docs/PENDING-20260910-グラフの項目列と転記のキー列.md` に発火条件つきで置いた）。

### 数字

```
効果の行列 245 件（★ 2 回ぶん載せる。片方だけ載せるとどちらを選んでも嘘になる）

  09-09 20:35   ✓ 242  ？断り 3  × 失敗 0   意図どおり 98.8% ／ 壊していない 100.0%
  09-10 11:30   ✓ 243  ？断り 0  × 失敗 2   意図どおり 99.2% ／ 壊していない  99.2%

  差の中身:
    ✓ が 1 増えた   この版の直し（頼まれていない値を書かなくなった分）
    × が 2 増えた   上の②グラフ・③転記。★ どちらも**この版で入った欠陥ではない**
                    ── HEAD（この版の変更を外した状態）でも 3/3 で再現する。
                    検体が 2 件しかないので、09-09 の 1 回はたまたま通っていた

  ★ 「壊していない 100.0%」は **1 回の測定**だった。3/3 で落ちる今日の測定の方が
    強い。数字を良く見せるために古い方を残さない。
全域 3,742 緑（実機 78 件を含めて 3,820）
```

## [v0.2.3](https://github.com/namakoo-dev/ailine/compare/v0.2.2...v0.2.3) - 2026-09-07

**「✓ の嘘」を 2 種類、機械で塞いだ**版。27 commit / 87 ファイル / +8,621 行。
どちらも同じ形の事故だった ── **依頼に在るものが宣言から落ち、宣言と実体は一致するので
事後条件が通り、`✓` が出る**。事後条件は「宣言 vs 実体」しか見ないので、
**依頼が痩せた分は原理的に見えない**。

### ① 依頼の語が解釈に出ていなければ `✓` を名乗らない

書く前に画面へ出す「解釈行」と、依頼文を突き合わせる。依頼に在る**列名**が解釈のどこにも
出ていなければ、`✓` を出さずに `⚠` を 1 行出す（★ 止めない・直さない）。

★ **本物の走行記録 3,179 件の上で測った**（模型ではなく）。鳴ったのは **28 件（0.88%）で、
そのすべてが本物の欠陥**、誤爆は 0。しかも自然実験になっていた ── 同じ依頼が、
欠陥の入っていた期間だけ鳴り、直った瞬間から黙る:

```
09-04 06:26〜09-05 03:34   op=ADD_ROW    条件つき書換を「行追加」と誤読 → 鳴る
09-05 06:01 以降           op=SET_WHERE  正しい                        → 黙る
```

★ **事後条件は前後どちらも `pass`** と言っていた。見えなかったのが事後条件で、
見えたのが残差だ。再現は `bench/residue_gate_on_history.py`。

★ 測り方も 1 段変わった。宣言は引数の辞書ではなく**人に見せている解釈行**を使う ──
「ナットの行を削除して」は引数だと `row=3` に化けて『ナット』が消えるが、解釈行には
「位置の根拠:『ナット』の行」として残る。引数側で測った版は誤爆 21%、解釈行側は 0.88%。

### ② 否定は「文に在るか」でなく「**何に付いているか**」で読む

本物の CLI を `--dry` で 14 件走らせたら、**逆向きの反転が 2 通り**再現した:

| | 依頼 | 旧 | 実害 |
|---|---|---|---|
| Q1 | 所属が営業**でない**行のメモに○ | `等しい` | 否定が消え、**営業の行**に書く（4/4） |
| Q2 | **メモ以外**は変えずに、所属が営業の行のメモに○ | `のどれでもない` | **営業でない行**に書く（3/3） |

判定を 3 通りに割った ── **値に付けば否定 / 列名に付けば条件の話ではない /
どちらでもなければ決めない（断る）**。3 つの口（条件つき書換・抽出・読み直し）すべてに
配線し、1 本の試験で 3 つとも縛った。

★ 扱う言い方も広げた: 「〜でない」「〜ではない」「〜じゃない」「〜を除く」「〜を除いて」。

### 条件つき書換が実用になった

- **2 つの条件（AND）** ──「所属が営業で担当が主任の行のメモに○」。
  ★ 判定は 1 条件のまま**2 回呼ぶ**（Basic 側も `RowMatches` を 2 回）。
  どちらの実装も無傷のまま、本体と検算が**別実装**である性質を保つ
- ★ 2 組目に採るのは「**条件の形で書かれた値**」だけ。
  「田中さんのように、所属が営業の行…」の『田中』は氏名列の実在値だが、条件ではない ──
  採ると 3 行が 1 行に縮む（自作レビューが致命として拾った）。
  採らなかった候補は**黙らない**（`⚠` を出して `✓` を降ろす）
- **「〜以外」** ── 値の一覧の否定として読む。どの値の否定か決められなければ**断る**
- **文字列の条件** ── `eq` が必ず数字を要求していたのを直した（兄弟の抽出は既に受けていた＝片配線）
- **3 つ以上／1 列に複数の値**は**断って道を案内する**。案内の列名・値・行数は
  **すべて実表から作る**ので、でたらめが入る余地が原理的に無い

### `✓` を降ろす条件が増えた

- **走査が表の終わりに届かなかった run** は `✓` も `△` も名乗らない
- **式が値に潰されたら言う** ── 並べ替えなどで数式が消えた列を名指しする
- **表を別の形へ写す 7 op**（集計・ピボット・帳票・様式写像ほか）に
  「**何を保存しないか**」を宣言させた
- **`basrun` が何も出力せずに終了した回**を、空のエラーとして返さない。
  空文字は「エラーが無い」ではなく「黙って死んだ」── 終了コードを添えて名指しする

### 断り方と聞き返し

- **無いシート名は機械が名指しする** ──「売上シートとは何シートですか？」を消した
- **op の軸に「まだ扱えないもの」を宣言させた** ── 提案のずれを、断りの質で解く
- **導線に出す例は「そのまま打てば通る」ものだけ** ── 自分の示した例を自分で断っていた
- **疑わしい語の属性**を、実表から作った選択肢で聞いて覚える

### 大きな関数を畳んだ（挙動は 1 バイトも変えず）

| 関数 | 前 | 後 |
|---|---|---|
| 読み直しの層（`_translate_and_dispatch`） | 681 行 | 207 行 |
| Basic を組む所（`codegen_dsl`） | 607 行 | 41 行 + 名簿 |

★ **順番を守った** ── 畳む前に検体を積む。読み直しは配線を通す検体が **3/15 しか無かった**
ので、13/15 まで積んでから畳んだ。生成される Basic は 1 バイトも変わっていない。

### 外からの目で見つかったもの

- **盲検の査定 9 件**。最重は「README が『この 2 つの数は機械が突き合わせている』と
  書きながら、実機テスト数が素テキストで誰にも守られておらず実測と 2.3 倍ずれていた」──
  **評価者が最初に打つコマンドが README を反証する**形だった。
  ★ もう 1 つ:「外に 1 バイトも出ない」が**宣言だけで機械が守っていない唯一の契約**
  だった。ollama の URL を作れる場所を 1 本に畳み、既定で外部を拒む
  （`--allow-remote-model` で人が明示した回だけ通し、その回も何がどこへ出るかを開示する）
- **自作レビューの確定 6 件**（致命 3・重大 1・軽微 2）。すべて処置済み

### 測り方

- ★ **模型ではなく、本番が残している記録の上で測る**。自前で翻訳と解決を呼ぶ模型は
  246 検体中 155 件しか届かず（実物は 238 件を通す）、落ちた 91 件を黙って捨てると
  誤爆率が**楽な側に偏った分母**で出る
- **素の環境で押す前に走らせる**（宣言外の依存を遮断）＋ **実機テストの後始末**

### この版で**できないこと**（隠さない）

- **抽出の複数条件**（「所属が営業かつ内線が200以上の行を抜き出して」）── 語彙に無い。
  断って道を案内する。効果の分母では**別枠**で数える
- **落ちたのが値だけの事故** ── ① の関所は**列名しか見ない**。
  値まで広げた版を測ったら誤爆が 21% に跳ねたので採らなかった
  （`bench/residue_gate_probe.py --values` で再現できる）
- **語が 1 つも落ちない誤り** ── 否定の反転のように、依頼の語がすべて解釈に出ていて
  意味だけが逆な回は、① では原理的に見えない（② が別の口から受け持つ）
- **1 文字の値**（『主』など）は 2 組目の条件に採れない ──
  ただし「採り損ねたこと」は ① が見せる

### 数字

- 試験 **3,552 本**（うち実機 67 本 ── CI では走らないので、押す前に必ず走らせる）
- 効果の行列: **245 件中 意図どおり 238・断り 7・失敗 0**
  ★ この 3 つ組は **2026-09-05 の実測**で、この版の変更後には測り直していない

## [v0.2.2](https://github.com/namakoo-dev/ailine/compare/v0.2.1...v0.2.2) - 2026-09-04

**測れていなかった 21 op を分母に載せ切った**版。11 commit / 17 ファイル / +3,763 行。
機能はほとんど増やしていない ── 増やしたのは**測る側**と、そこで見つかった穴の修理だ。

### 効果で測れる op が 8 → 29（全部）になった

「op 名が合っているか」ではなく「**出来上がったファイルが意図どおりか**」で測る検体を、
**160 → 245 件**（＋別枠 1）へ広げた。

| 段 | 載せた op | 件数 |
|---|---|---|
| 1 | 書式 7 op（太字・けい線・数値書式・背景色・列幅・セル結合・中央揃え） | +30 |
| 2 | 行/列を動かす 6 op（合計行・列を一律・条件つき書換・空行・重複除去・セル分割） | +31 |
| 2.5 | 条件の指し方（文字列/数値・否定・OR） | +9 |
| 3 | 新しい表を作る 7 op（集計・ピボット・列抽出・グラフ・参照埋め・帳票・様式写像） | +15 |

★ **毎回、詰まっていたのは op ではなく検体の側**だった。計算列が測れなかったのは数値の列が
1 本しかない表しか無かったから。書式が測れなかったのは値の格子に出ないものを読む口が
無かったから。重複除去とセル分割が測れなかったのは、重複行も区切り入りセルも表に無かったから。
**4 回とも「分母が痩せている」が真因**で、表と読む口を足したら測れた。

### 塞いだ穴（どれも `✓` が嘘になっていた）

- ★★ **依頼文に無い列へのすり替えに `✓` が出ていた**。「**原価**の列を削除して」（その表に
  原価は無い）に対し、模型が対象を `備考` に差し替え、機械は「実在する列だから」と通し、
  **✓ 機械検証済みを出して備考を消していた**。4/4 で再現。⚠ ですらない静かなデータ喪失。
  ★ 関所は在ったのに鳴らなかった ── 絞り込みが「〜**で**」の形しか掴まず、「原価**の**列を」は
  素通りしていた。**助詞で当てるのをやめ、宣言で分けた**（「この op は列を新しく作るか」は
  既に宣言に書いてある。作る op なら無い名前を言うのが正しい）
- **区切りを名指ししても無視されていた**。「連絡先を**読点で**分けて」に対し模型が `,` を返し、
  「カンマが見つからない」で止まっていた。区切りは依頼文から機械が取る
- **条件つき書換が文字列を受けられなかった**。`eq` が必ず数字を要求していて、
  「所属が営業**と等しい**行に○」が構造的に表現できなかった
  （兄弟の抽出は「数値化できれば数値・できなければ文字列」と明記して受けている＝片配線）

### 測り方そのものを直した

- ★ **「断るはず」を分母に載せた**（`MustRefuse`）。判定が 成功/断り/失敗 の 3 値だと
  **断りが正しいかは人が読む**ことになり、「断るはずの依頼が通っても数字が 1 も動かない」──
  いちばん危ない向きの事故が素通りしていた。実際その形の穴が 1 件見つかった
- ★ **主張の範囲を切った**。「語彙に無い」と明記した操作（複数条件 AND）は本体の分母から
  外し、**同じ画面に必ず併記**する。逆に**語彙の中にあるのに失敗する 2 件は分母に残した**
  ── 外せば「壊していない 100%」に戻るが、それは検体を甘くしただけになる
- ★ **測定器そのものを試験にした**（27 本）。段2 の検算 6 本のうち **2 本が最初は間違って
  いて**、どちらも**正しい出力を × と読む**向きに壊れていた（合計は数値でなく `=SUM(...)` の
  式で来る／空セルは `None` で来る）。製品は無実だった
- ★ **揺れを 4 回測って記録した**。2 回続けて完全一致したので「揺れ 0」と書いたが、
  3 回目で崩れた ── **一致は分散の上限を示さない**。観測（`221/221/219/220`）を記録に残す形にした

### 番人

- **古い分母の禁止** ── 「N 件中」の N が記録と違えば赤（★ 白名簿は `(文書, 値)` の対で持つ。
  数字だけだと、ある紙のための許可が全部の紙に効く）
- **主張の禁止** ── 記録に失敗が在る間は、どの文書も「壊していない」と書けない
- ★ 立てた番人はすべて**変異試験**を通している（わざと壊して赤くなることを確認）

### 文書

- `docs/開発手法.md`（新設）── **他のプロジェクトへ持っていける手順**を 23 項目・4 群で。
  製品の設計記録（`docs/ENGINEERING.md`）とは別立て。
  ★ 原則は一行、長い説明は**事故の側**に書く。実物の在り処を必ず指す

### 数字

- 試験 **3,163 本**（実機 41 本を含む）
- 効果の検体 **245 件**（意図どおり 96.3%・壊していない 99.2%）＋ 別枠 1 件
  ★ 率が下がったのは健全 ── 下がった分は、それまで測れていなかった言い方を分母に入れたぶんだ。
  **難しい検体を足して数字が上がるなら、検体が甘い**
- 未解決 2 件を分母に残している（比較語の無い条件が行追加に化ける）

## [v0.2.1](https://github.com/namakoo-dev/ailine/compare/v0.2.0...v0.2.1) - 2026-09-03

表計算の**基本操作を出し切り**、そのうえで**単一ファイルの分割**に着手した版。
92 commit / 212 ファイル / +23,463 行。主眼は前回までと同じ ──
**`✓` が嘘になる経路を塞ぐ**こと。今回はそれに「**壊した時に気づける形にする**」が加わった。

### できるようになったこと

- **表の基本操作が揃った**: `SWAP`（行/列/セルの入れ替え・★ 軸は LLM に当てさせず実表を見た
  機械が決める）/ `ADD_COLUMN` / `SET_WHERE`（条件つき書換）/ `EXTRACT_COLUMNS` /
  `SPLIT_CELL` / 置き換え / `redo`。**29 op** になった
- **「〜以外」の抽出**（`cmp=nin`）── 空欄は残し、部分一致にしない
- **書く前に「こう読みました」を見せる**: 表記ゆれを 1 つずつ矯正する道（treadmill）から降り、
  **人が照らす機会を書く前に渡す**形へ。事後条件は宣言と実体しか照らせない ──
  依頼と宣言を照らせるのは人だけなので
- **帳票**: 同じ取引先の複数発注を請求書 1 枚にまとめる（`REPORT_PER_ROW` / `FORMAT_MAP`）

### 塞いだ穴（`✓` が嘘になっていた経路）

- **入れ替えで ✓ を出したままデータが 1 つ消える**（1 セル書換に読み替えていた）
- **合計行が並べ替えで本体に沈む** / `#REF!` で壊れる / 列の入れ替えで見出しが動く
- **行を指した依頼で列全体が書き換わり ✓ が出る**
- **LibreOffice で開いたまま書き換える**（Excel だけ守られていた）
- **読み直しが「失敗の形」で鳴っていなかった**（列抽出・条件つき書換が到達不能）

### 構造 ── 単一ファイルの分割に着手

`src/ailine/__init__.py` を **17,671 → 15,085 行**（-2,586・14.6%）。
`ailine_core` は 38 → 46 モジュール。

- `postconditions/{shape,move,derive}.py` ── 事後条件 28 op を「表に何をする op か」で 3 群へ
  （★ 29 op を過不足なく分けられることを機械が検算している）
- `table_scan.py` / `primitives.py` / `postconditions/_shared.py`
- ★ **写し取られた実装を畳んだ**（`_is_number` は 7 箇所にあった）。ただし `sum_identity` は
  「標準ライブラリだけで閉じる」契約を持つので**あえて写しを残し**、
  写し同士が一致していることを番人が守る ── **重複が悪とは限らない**

### 壊した時に気づける形にした（番人）

- **公開面 675 名の凍結** ── 分割で名前が消えても差分に出ないので、顔ぶれを数え上げる
- **同名定義の台帳** / **改行の混在の台帳** / **番人を数える番人**
- **依存の図**（`docs/依存関係.md`・46 モジュール 70 辺・層の違反 0）を実体から生成し、
  図と実体の一致を機械が守る
- ★ 立てた番人には**変異試験**をかけている（わざと壊して赤くなることを確認）

### 文書

- `docs/なぜこの形か.md` ── CSV / DB を選ばなかった理由。★ SQL との比較を**実測**に置き換えた
  （`bench/sql_invariant_demo.py`: CHECK 制約を全部通りながら表が矛盾する。
  主張は「SQL より上」ではなく **「SQL にも同じ層が要る」**）
- 提出資料を README 1 本に畳んだ

### 数字

- 試験 **3,122 本**（実機 41 本を含む）/ 効果の検体 **160 件**（意図どおり 98.1%・壊していない 100%）
- ★ どの数字も専用の試験が実測と突き合わせている（人が書いた数は必ず古くなるので）

## [v0.2.0](https://github.com/namakoo-dev/ailine/compare/v0.1.1...v0.2.0) - 2026-08-27

盲検のレビュー 5 面で出た**致命 10 件を全件処置**し、表計算の基本操作と GUI を足した版。
主眼は前回と同じ ── **`✓` が嘘になる経路を塞ぐ**こと。

### できるようになったこと

- **表の基本操作**: `ADD_ROW`（値を入れた行を 1 本追加）/ `DELETE_ROWS` / `DELETE_COLUMN` /
  `SET_CELL_VALUE`（1 セルだけ書き換える）。削除は**消す前に中身を見せる**
- **位置を相対で指せる**: 「みかんの下に」「みかんとぶどうの間に」「梨の行」。
  0 個または 2 個以上に当たったら断る（黙って 1 つ選ばない）
- **GUI**（`python gui/server.py`）: 操作前後の表を書式つきで並べ、判定バッジ・変更点・
  `⚠` を出す。下書きは逐次保存し、undo で 1 つ戻せる。確認（y/N）にも画面から答えられる。
  ★ 判定は 1 ビットも作らない ── `ailine run --json` の `verdict` をそのまま映すだけ

### 直した「嘘」（盲検で出た致命）

- **原本が 0 バイトになるのに「変更していません」と報告する**経路があった。
  置換のフォールバックが原本を truncate していた ── フォールバックごと削除した
- **検算が恒真だった**: 合計行の判定に、書き手と同じ関数を使っていた。
  分母を独立読み実装から取り、落とした行は判定を変えずに**開示**する形にした
- **関所が空振りしていた**: `out_sha` のゲートが履歴に載らず、一度も発火していなかった
- **`export-pdf` が出力先の同名 PDF を壊していた**（soffice を専用の一時領域に閉じた）
- **時刻セルが 1899 年になっていた**（読み手を 2 つ用意して突き合わせたら出た）
- **キャッシュの無い数式が空欄で書き出され「欠落 0」**と報告していた
- **PDF 照合が素の部分文字列**だった（位置と件数を数える形にした）
- **壊れた `.xlsx` で生の traceback**（4 経路のうち 2 経路）

### 文書

- README を提出用に書き直し、それまでの README を `docs/ENGINEERING.md` へ移した
- **`pytest -m local` の本数の嘘を直した**（3 箇所で「1 本」・実測 14 本）。
  以後は `tests/test_local_test_count.py` が実測と突き合わせる

## [v0.1.1](https://github.com/namakoo-dev/ailine/compare/v0.1.0...v0.1.1) - 2026-08-24

盲検のレビュー（役割を分けた 4 者に、過去の点数も直した箇所も渡さずに投げた）で出た
所見 20 件のうち 17 件を処置した版。**`✓` が嘘になっていた経路を塞ぐのが主眼**。

### できるようになったこと

- **期間で絞ってから集計できる**。「2026/3/26 から 4/25 までの分だけ現場ごとに売上を集計して」が
  1 コマンドで通る。複合計画の後段が前段の出力を対象にするようになった
  （連鎖したことは「（1段目の出力『日付2026_3_26以上』を対象にしました）」と毎回表示する。
  依頼文に「〜シート」と書けば人の指定が勝つ）
- **日付の列を並べ替えられる**。従来は LibreOffice が正しく並べても検証側が
  「数値でない」と見て拒否し、原本に反映していなかった
- **日付の範囲抽出**（`gte`/`lte`）。締め日は**両端を含む**。時刻つきの列でも「4/25 23:00」は 4/25 に入る
- **`SPLIT_CELL`**: 1 セルに詰まった複数値を区切りで右の列へ割る。
  `✓` の根拠は「割った断片を同じ区切りで繋ぎ直すと元と一致する」ことだけ
- **`ailine export-pdf`**: ブックを PDF へ。出した PDF の**テキスト層を読み戻して**
  元の値が載っているかを数える。`--fit-to-width` / `--orientation` / `--sheet`
- **`ailine export-csv --out` と上書きの関所**（従来は同名で黙って上書きしていた）
- README に帳票・様式・分割・期間の節を追加（`{{列名}}` の仕様が書かれていなかった）

### 直した「嘘」

- **存在しない列を頼まれたら聞き返す**。「金額で降順に並べ替えて」に対し、金額列が無い表を
  **数量で並べ替えて `✓ 機械検証済み`** と表示していた。今は
  「依頼文の『金額』は、シート『売上表』の列にありません。ある列: …」と断る
- **1 列目に空欄があると数え落とす**のを黙っていた。5 行の表が 3 行と数えられ、
  それでも「データ N 行 → 出力 N 枚」の完全会計が成立していた（分母を計画側と検証側が
  同じ関数で作っていたため恒真）。今は縮んだ事実を必ず言い、`✓` は `△` に落ちる
- **1 セルに印が 2 つある雛形**を、2 つ目を生のまま残して出力していた（顧客に出す紙に
  `{{担当者}}` が印字される）。今は埋めずに断る
- **ふりがなが本文に混入**していた（日本語版 Excel が自動で埋める読み仮名を拾い、
  「山田太郎」が「山田太郎ヤマダタロウ」に）。照合も同じ読み方だったので恒真だった
- **`export-pdf --sheet` が効かず**、帳票段で作った全社分の請求書が 1 つの PDF に入っていた
  （他社の情報を同封して顧客に送る事故）
- **雛形の A1 が空だと比較セル数 0** のまま「雛形は無変更」を宣言していた
- **セル分割の検算が列ずれを見逃す** / **PDF 照合の分母から数式セルが消える**
- **`export-csv` がディスクでなくメモリを読み戻していた**（書き込みが途中で切れても `✓`）
- **依頼文に無い年を LLM が捏造**（「3月26日から4月25日まで」に 2023 年）
- **PDF 照合が書式なしの値**で行われ、正しい PDF に `×` が出ていた（`1,000` / `¥1,000` / 日付）
- **「取引先ごとに」で同じ相手の書類が別々の枚に分かれる**のを黙っていた

### 直したドキュメント / 表示

- **README の設置コマンドが壊れていた**: `setx BASRUN "%CD%\basrun\basrun.py"` の `\b` が
  バックスペース(0x08)として焼き込まれていた。全ファイルを機械走査して 7 箇所を復元し、
  再発を止める番人を常設した
- 「根1」等の開発者用語が正常系で毎回鳴っていたのを、買い手の言葉に書き換え
- README の「16 桁の JAN を守る」という誇張を実測に合わせて訂正（実物の JAN は 13 桁）

### 内部

- 台帳（実案件 60 件の調査）の「不足 op」を実測で測り直し、6 件は既に届いていたことを確認
- 検体 1,838（すべて実装より先に凍結）

## [v0.1.0](https://github.com/namakoo-dev/ailine/commits/v0.1.0) - 2026-08-23
