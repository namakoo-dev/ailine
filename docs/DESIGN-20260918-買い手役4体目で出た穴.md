# 買い手役 4 体目で出た穴 ── 記帳代行の担当者（2026-09-18）

役: 記帳代行の事務所の担当者。冊: 顧問先の請求書 5 通 ＋ 9月仕訳（勘定科目が空欄）＋ 過去の仕訳。
条件は 1〜3 体目と同じ ── **直した箇所・こちらの心配・過去の結果・値段の前例は渡していない**。
GUI は禁止（Namakoo の決定で別途本人が確認する）。

★ この文書は **2026-09-19 に書いた**（当日は `tests/blind_runs/004/README.md` だけを残し、
所見の本体を書き損ねていた ── Namakoo「いまのうちに書く方針」）。

★★ **この回の分母は欠けている。**`trace.jsonl` が無く、打った全コマンドは買い手の報告文に
しか残っていない。`AILINE_TRACE` を買い手役のシェルに渡し損ねたため（こちらのシェルで
export しても別プロセスには継承されない）。詳細は `tests/blind_runs/004/README.md`。

---

## 出た穴と、いまの状態

| # | 穴 | 状態 |
|---|---|---|
| ① | **台帳が画面と違う語を言う** ── 画面が `△`（⚠ 1 件）なのに `ailine history` は `✓` | **直した**（`3f7ec5b`） |
| ② | **請求元と宛先の取り違え** ── 発行元が宛先の欄に入り、請求元が空 | **保留**（`PENDING-20260918-請求元と宛先の取り違え.md`） |
| ③ | `ailine verify` が②を `✓` で通す（値が**正しい列に在るか**を見ていない） | 保留（②と同じ根） |
| ④ | **1 セルの依頼が空の列を潰す** ──「セル E1 に 借方金額 と入力して」が一括書換になり D2〜D7 を上書き・`exit 0` | **直した**（`99a508b` / `d75da52`） |
| ⑤ | `accounts` の**列名の壁** ── `--key` / `--column` が無く、エラーが原因を誤診する | 未処置 |
| ⑥ | **`doctor` がバージョンを一言も言わない** | 未処置 |
| ⑦ | 画面の語が開発者向け（「宣言どおり」「事後条件」など） | 最終調整へ持ち越し（Namakoo 決定） |
| ⑧ | README が 700 行で、買い手が読む所を見つけられない | 同上 |

---

## ① 台帳が画面と違う語を言う（致命・直した）

```
画面:  △ 使い捨て2.out.xlsx は宣言どおりの変化を確認しました ── ただし ⚠ 1 件を…
直後の ailine history:
       2026-09-18T08:43:20  ✓  1  qwen2.5-coder:7b  使い捨て2.xlsx  セル E1 に 借方金額 と入力して
```

★ 4 回とも再現。台帳の実体にも `"ok": true` としか残らず、**⚠ の痕跡が無い**。

> 買い手:「顧問先の帳簿を触る道具で、**作業記録が嘘をつく**のは致命的です。税務調査で
> 『この仕訳は何を根拠に入れたか』を説明する時、私はこの履歴を見ます。『機械検証済み』と
> 書いてあるのに実際は警告つきだった、では使えません。**この 1 点だけで、私は所長に
> 導入を提案できません**」

**根は片配線。**`verdict` は `_finish_apply` が 1 箇所で正しく決めていた
（verified / warned / unverified / unobservable）。コメントにも「決めるのは 1 箇所・
映すのは何箇所でも」と書いてある。ところが **`build_history_entry` が verdict を
1 つも載せていなかった** ── 映す側は読みようが無く、別の真偽値（`ok` ＝適用が通ったか）で
二値に潰していた。`ok` と「機械検証済みか」は意味が違う。

検体: `tests/test_the_ledger_says_what_the_screen_said.py`。

---

## ④ 一番埋めたい列が、一番無防備（致命・直した）

```
依頼: セル E1 に 借方金額 と入力して
解釈: 一括書換  対象列:借方勘定科目
結果: D2〜D7 を 'borrow金額' で潰す（原本直接）・確認なし・EXIT=0
```

> 買い手:「**一番埋めたい列が、一番無防備**」

★ 潰された列は**空だった**ので、既存値を守る上書きの関所も鳴らない。

**根は「A1 形式が読めない」こと。**器官に直接訊いたら三項が全部立っていなかった:

```
行番号を名指ししているか : None
値を引用しているか       : None
セルを解けるか           : None
```

★★ そして致命は「読めない」ことではなく **読めないことと、指していないことが
区別できない**ことだった。`task_points_at_one_row` が `None` を返す時、それは
(a) 依頼は本当に 1 か所を指していない → 一括書換でよい、
(b) 指しているが読めない記法だった → 一括書換は事故、の**2 通りの意味を持つ**。
画面上この 2 つは同じ顔をする。

**直し**: A1 記法（`E1` / `b3`）と表の端（最終行・一番下・末尾・一番上・先頭）を読めるようにし、
**依頼の項に分母を作った**（`tests/notation_register.json` ＋ `python scripts/notation_board.py`）。
読める記法・読めない記法（在庫）・陰性対照を名簿に持ち、実物に検体を通して照合する。

---

## ② 請求元と宛先の取り違え（保留・2 回失敗した）

```
元ファイル             | 請求元 | 宛先               | 請求額(税込)
請求書_大東金属.xlsx   |       | 大東金属 株式会社   | 141900
```

大東金属は**発行元**。宛先は顧問先「みどり商事」。発行元が宛先の欄に入り、請求元は空。
全 5 通で `請求元: 無 5`。

> 買い手:「請求元が全部空・宛先に仕入先名という表は、**そのままでは紙より悪い**
> （間違った情報が入っているので）。**この機能は無いのと同じ**です」

★ 区分は `単`（＝値を出す）で出ている ── README の「空欄は誤値より安い」に反する。

2 案を当てて 2 案とも **305 冊の基準線が退行を止めた**（案 A で 225 冊・案 B で 16 冊が悪化）。
当て推量で 3 回目を撃たずに止めた。根・失敗の記録・次に当てる時の線は
`docs/PENDING-20260918-請求元と宛先の取り違え.md`。

---

## 値付け（★ 過去の前例は渡していない）

**買い切り ¥15,000 / 月額 ¥1,000**。ただし月額は条件つきで **¥5,000** まで
（4 人の事務所を想定）。

★★ **3 体目と 4 体目は、どちらも月額の条件を「嘘をつかないこと」に置いた** ──
機能の追加ではない。3 体目は買い切り ¥10,000＋条件つき月額 ¥3,000〜5,000 で、
同じ形の条件を出している。

★ 役も冊も違うので**統制された比較ではない**（1 体目 ¥3,000 / 2 体目 ¥0 /
3 体目 ¥10,000 / 4 体目 ¥15,000）。数字の並びを傾向として読まないこと。

---

## この回の汚染（★ こちらの落ち度）

1. **入っていた `ailine` が v0.1.0（古い版）だった。**こちらが版を確かめずに渡したため、
   買い手は「README に在るコマンドが無い」で 30 分溶かしている。
   ★ ただしそこから本物が出た ──「**`doctor` がバージョンを一言も言わない**」（⑥）。
2. **`AILINE_TRACE` が買い手役のシェルに継承されていなかった。**「環境を整えてある」と
   言ったが、確かめていなかった。この回は `replay` / `floor` / `sheet` に掛けられない。
3. ★ **買い手が打った依頼文そのものが残っていない。**2026-09-19 に揺れを測ろうとして
   気づいた ── `AILINE_HOME` を切っていたので本体の履歴にも無く、冊も一時ファイルで全滅。
   **何が到達して何が断られたかを、後から数え直せない。**

---

## 5 体目への申し送り

★★ **4 件すべて片づいた（2026-09-20）。**

  1・2 は `python scripts/blind_session.py check <体>` が**実際に 1 回走らせて**測る
  （設定を読んで安心しない・6 項目とも変異試験で赤くなることを確認）。
  3 は同じ道具の `prepare`/`freeze` が構造的に残す。
  4 は合格線に刻んだ（到達は 30 回振って合格率／嘘の到達は 1 度で不合格）。
  ★ その `check` が、**自分の道具のバグを 1 つ先に掴んだ** ── `AILINE_TRACE` は
    フラグでなく**パス**で、初版は `"1"` を渡していた（買い手のカレントに `1` という
    ファイルを作る）。「環境を整えてある」がまた嘘になるところだった。
  ★ もう 1 つ: 試し打ちの 1 行が買い手の履歴に残り、**冊の無い依頼として corpus に
    混ざって**いた ── 測定が対象を汚す形。バイトごと戻して塞いだ。

1. **買い手役の環境そのもの**に `AILINE_TRACE` と `AILINE_HOME` を設定してから出す
   （渡したつもりで渡っていない、が 4 体目の取りこぼし）。
2. **入っている版が作業木と一致しているか**を、渡す前に機械で確かめる。
3. ★ **依頼文そのものを凍結する。**所見だけでは分母にならない。
4. ★ 揺れが在ると分かった以上（同じ依頼で op が変わる実測が 2026-09-19 に出た）、
   **「1 回通れば合格」なのか「N 回振って合格」なのか**を先に決める。
   盲検は分布を 1 回引いただけで、別の日なら別の欠陥が出る。
