# PDF の帳票を読む ── 設計（2026-09-12）

## §0 なぜ

実務で受け取る請求書は **PDF が圧倒的に多い**（Namakoo の判断）。ailine が読むのは xlsx だけなので、
需要①（帳票の山から項目を抜く）の射程がそこで切れている。

構造は有利だった。`form_grid` `form_read` `field_record` `forms_collect` `forms_suspect` `date_compare`
の **1872 行のうち、Excel を知っているのは 13 行**。残りは `Grid` の 8 つの口
（`right_of` / `below_of` / `text_cells` …）の上に載っている。

## §1 測ってから決めたこと（探針の実測・2026-09-12）

87 冊を LibreOffice で PDF 化し、**答えは Excel と同じもの**で採点した。器官（`form_read`）は 1 行も変えず、
PDF → `Grid` の変換器だけを書いた。

| 版 | 正 | 誤報 | 宛先 | 請求元 |
|---|---|---|---|---|
| 素朴な変換器 | 53 (31%) | **14** | 1/40 | 11/57 |
| ＋ 宛先未確定なら請求元を出さない（済・`967316c`） | 44 | 3 | 1/40 | 2/57 |
| ＋ **空白を捨てるのをやめた** | **142 (83%)** | 4 | **40/40** | **55/57** |

### 1.1 ★ 空白は「ここまでが同じセル」の証拠

```
PDF の中身   '合' '　' '　' '計'      ← Excel のセルなら「合　　計」
既定の抽出   '合' | '計'   ← 別の語になり「合計」の照合が永久に当たらない
keep_blank_chars=True → '合　　計' → norm が空白を落として '合計' ○
```

`x_tolerance` を広げても直らない（空白**文字**が実在するので位置の閾値の問題ではない）。
日本語の帳票は**均等割付**（合　　計・品　　名・請 求 書）が普通で、ここを外すと
帯も明細の見出しも一生見つからない。**このフラグ 1 つで 31% → 83.6%。**

### 1.2 実物 2 社で共通した罠

1. **`pdftotext`（poppler）は日本語を 1 文字も返さない。** 数字と記号は返すので「空でない＝成功」に見える
2. **漢字が康熙部首に化ける。** `請求⽇` の「⽇」は U+2F47（`請求日` は U+65E5）。
   Adobe 16 枚・Wondershare 1 枚のすべてで発生。見た目は同じなので目でも diff でも気づけず、
   **ラベル照合が全部外れるのにエラーは 1 件も出ない** → 取ったら**何より先に NFKC**

### 1.3 残った 6 件の正体 ── 2 種類に分かれた

★★ **最初の帰属は 2 件間違えていた。** 「T18・T24 は式が無いせい」と書いたが、
Excel 側で何が起きているかを実測したら違った。**commit 前に一拍疑って測り直して直した記録**として残す。

**(a) PDF が物理的に持っていない ── 直せない（4 件）**

| 失う情報 | Excel での振る舞い | PDF で起きること | 冊 |
|---|---|---|---|
| 数値 vs その通りの文字 | 「D13 の金額が**数値ではなく文字**（`¥1,320,000-`）」で**拒否** | 数に復元して値を出す | T21 |
| 同（全角） | 「D13 の金額が文字（`１，３２０，０００`）」で拒否 | 0 を出す | T34 |
| エラー状態 | 「D22 が `#REF!`（式が壊れています）」で拒否 | もっともらしい値を出す | T33 |
| 表示で丸められた精度 | 確 13579.5 | 13,580 と出す | T23 |

★ ここで見えた設計の含み: Excel 側は「金額の欄に**文字**が入っていたら拒否する」を
**意図してやっている**（人が手で打った数字は疑う）。PDF ではその区別が原理的に付かない ──
つまり **PDF から読む時、この番人は 1 つ失われる**。正直に台帳へ書く（D10）。

**(b) 格子の忠実さ ── 直せる（2 件）**

| 冊 | Excel での理由 | PDF で落ちた所 |
|---|---|---|
| T18 | `conflict=True`「明細の合計 45,000 と小計 30,000 が合わない（34 行目の税率が空）」 | 明細の列の割り当てが甘く、**食い違いを見つけられていない** |
| T24 | 3 つの根拠が食い違う（E12=109999／L36=109999／小計+消費税=110000） | 3 つ目（小計＋消費税）を組めていない |

★ どちらも**明細表の列をどう割るか**に帰する。ここが甘いと
「食い違いを見つけて拒否する」という一番強い番人が黙る。

## §2 決めたこと

### D1 置き場所 ── `ailine_core/pdf_grid.py`（新規）

`form_grid.Grid` は**格子の定義**のまま。`Grid.read(ws)` は openpyxl 用の名前付きコンストラクタとして残し、
`pdf_grid.grid_from_page(page)` を並べる。★ pdfplumber の import は `pdf_grid` の中だけ
── 任意依存を 1 モジュールに隔離する（居なければそのモジュールだけが使えない）。

### D2 空白と位置 ── 閾値は 2 つだけ、どちらも実測の谷から

- 空白文字がある所は**同じマス**（`keep_blank_chars=True`）
- 行は y の差 **4pt** でまとめる（本文 10〜11pt の半分弱）
- 列は中心の並びを **40pt** の隙間で切る（実測: 列内のばらつき ≤31 < 列間の隙間 ≥57 の谷）

★ 閾値を増やさない。増やしたくなったら、それは列挙で当てにいく合図。

### D3 値の復元 ── 描画の逆算だけ（推測しない）

| 描かれた文字 | 復元 | 理由 |
|---|---|---|
| `¥32,400-` `330,000円` `1,234` `1` | 数 ＋ 書式 | Excel なら「数値＋表示形式」だったもの |
| `000-0000` `00-0000-0000` `S-202504-001` `1個` `2025年4月30日` | 文字のまま | 数ではない |

★ 「裸の 1 を数に戻すと番号や年を壊す」と最初は恐れたが、**測ったら起きなかった**
（請求番号はラベルから文字として読み、日付は日付として読むので、裸の数が化けて入る経路が無い）。
逆に戻さないと**数量の列が空に見え**、器官が「単価は在るのに数量が空」と読んで合計を信じなくなる。

### D4 ★★ 写しを見分けられない時は「裏が取れた」を名乗らない

これが設計の核心。**PDF 固有の話ではない**ことを測って確かめた:

```
Excel を式なしで開く（read_book(wb) ── wb_formula を渡さない）
  → B01 の請求額の根拠が 2 つ → 3 つ に増える（写しを独立と数えている）
  → 87 冊では区分が変わる冊は 0 ★ 壊れている経路は在るが、この検体では点数に出ない
  → PDF では出る（下の「観測された被害者」）
```

★ **観測された被害者**（2026-09-12 に測り直して確定）:
  - Excel: **0 冊**（87 冊で区分が変わる冊は無い）── 壊れた経路は在るが、いまの検体に被害者は居ない
  - PDF: **在る** ── Wondershare の雛形で請求額が **確 32,400** を名乗った。
    上部の欄と帯の合計は（元の Excel なら）式で繋がった同じ事実のはずで、**裏取りは earned でない**

**処置**: 区分の導出に「写しを見分ける手段が在るか」を渡す。

- `field_record.Record` に `copies_indistinguishable: bool = False`（＋ 理由）
- `grade_of(evidences, swept, conflict, copies_indistinguishable)` は、真なら**裏取り済みの区分を出さない**
- ★ 区分の語も導出も `field_record` の中だけ（AST の番人が縛っている契約を守る）

**どこで真になるか（1 箇所に畳む）**:

| 入口 | 値 |
|---|---|
| Excel ＋ 式のブック | False |
| **Excel ＋ 式のブックなし** | **True**（潜在欠陥をここで塞ぐ） |
| **PDF** | **True**（式が存在しない） |

★ 片配線を作らない ── 「PDF だから」でなく「**写しを見分ける手段が無いから**」で書く。
  次に別の入口（CSV・OCR）が来ても同じ 1 箇所で決まる。

### D5 番地の書き方

PDF は `2頁12行3列`（人が原本を開いて指で追える形）。Excel は今までどおり `H20`。
★ 検分シートと所見に出るので、**原本に戻れる**ことが条件。

### D6 どのページを読むか ── シート選びと同じ線

`invoice_signals(grid)` が 2 つ以上の印を返すページで、単独勝ちのものだけ。
★ 答えから受け取る口は作らない（Excel 側で一度やって消した過ち）。

★ **明細が複数ページにまたがる形は設計しない。** 検体に 1 冊も無いので測れていない。
発火条件つき保留 ── 2 頁目に明細の続きがある実物が来たら、その時に測ってから決める。

### D7 テキスト層が無い PDF（スキャン）

読めないと**名指しで**言ってスキップする。**OCR はしない**（射程外）。
★ 「読めなかった」と「請求書ではなかった」を混ぜない ── 理由を分けて出す。

### D8 pdfplumber は**必須依存にする**（Namakoo 決裁 2026-09-12）

俺の初案は「任意のまま」だったが、**PDF が本流なら主機能が既定で使えないのはおかしい**という
判断で必須にする。増える環境依存の実測:

```
pdfplumber 0.11.9
  ├ pdfminer.six  9.4 MB ─ cryptography 10.9 MB（コンパイル済み）─ cffi, pycparser, …
  ├ pillow        16.1 MB
  └ pypdfium2      8.2 MB（コンパイル済み）
合計 47.5 MB / 9 パッケージ  ── 宣言は openpyxl 1 本 → 10 本
```

普通の pip（Windows / macOS / Linux・x86-64 / arm64）なら全部 wheel が在るので一発。
引っかかるのは air-gapped の社内機・古い Python・変わったアーキテクチャで、
コンパイル済みの 2 本（cryptography・pypdfium2）がそこに効く。

★ **入れると得が 2 つ増える（PDF を読む以外に）**:

1. `ailine export-pdf` の検算が**常に**効く ── いまは pdfplumber が任意なので、
   居ない環境では PDF を作っても「✓ を名乗らない（⚠ 機械保証はありません）」で終わっている
2. **Pillow が宣言済みになる** ── 「写真の印は PDF に残る」は Pillow が在る時だけの真実で、
   宣言していない以上**買い手の環境では嘘**だった（`ci_parity` を作った時に掴んだ製品のバグ）。
   宣言することで構造的に閉じる

**配線**: `pyproject.toml` の `dependencies` と `requirements-dev.txt` の両方。
★ 後者は素の環境の番人（`scripts/ci_parity.py`）が読む正なので、片方だけでは片配線になる。

### D9 `ailine forms` の入口

`multifile.classify_folder_contents` が `.pdf` も候補にする。
★ Excel と PDF が混ざったフォルダでも**束で疑える** ── 同じ取引先が月によって
PDF と Excel で来ても照合できる（むしろ利点）。

### D10 検体と採点

- 変換: LibreOffice で 138 冊 → PDF（bench に手順を置く・生成物は gitignore）
- 採点: **答えは既存のものを流用**（PDF 化しても正解は変わらない）
- ★ **「PDF では持ち得ない情報」の台帳**を作る（1 件ずつ理由つき・件数の上限つき）。
  これが無いと §1.3 の 6 件が永久に赤いまま残り、**追いかけて正しい実装を壊す**方へ手が動く。
  battery の `known_flaky` と同じ作法（原因未同定なら「未同定」と書く）

## §3 段取り（取り返しの付きにくい順）

1. **D4** ── `field_record` ＋ `form_read`。**PDF と独立に価値がある**（Excel の潜在欠陥を塞ぐ）ので先
2. **D1〜D3・D5** ── `pdf_grid`
3. **D6・D7** ── `read_pdf_book`（ページ選び・テキスト層なしの名指し）
4. **D9** ── `forms` の入口と報告
5. **D10** ── bench（変換手順・採点器・持ち得ない情報の台帳）

## §4 番人（各段に陽性・陰性対照と変異試験）

| 守るもの | 変異したら赤くなること |
|---|---|
| D4 | 式を渡さずに開いた冊で「裏が取れた」を名乗らない（処置を外すと赤） |
| D2 | 空白を捨てる設定にすると帯の合計が見つからなくなる |
| D3 | 裸の数を戻さないと数量の列が空に見える／`000-0000` を数にしない |
| D6 | 請求書でないページを選ばない・複数候補で単独勝ちしない時は選ばない |
| D7 | テキスト層が無い PDF は「読めなかった」と名指しする（黙って 0 件にしない） |
| 全体 | 検体で**誤報 0**（PDF で持ち得ない情報の台帳に載っているものを除く） |

## §5 やらないこと（理由つき）

| やらない | 理由 | 発火条件 |
|---|---|---|
| OCR | テキスト層が無い PDF は射程外。精度が確率になる | 実需で紙のスキャンが来た時 |
| 複数ページの明細の結合 | 検体に 1 冊も無い（測れていない） | 2 頁にまたがる実物が来た時 |
| ラベルの言い回しを足す（`御請求金額` 等） | 列挙は足し続けないと当たらない。`御`/`ご` のような**閉じた変換**なら別途測って決める | 同じ形で 2 社目が出た時 |
| PDF から「確」を出す | §1.3(a) ── 式が無いので写しを見分けられない | 式に相当する裏取りの手段が見つかった時 |


## §6 実装記録（2026-09-12・段取り 1〜5 まで）

| 段 | 入れたもの | 番人 |
|---|---|---|
| 1 D4 | `field_record.Record.copies_indistinguishable` ＋ `form_read.copies_are_indistinguishable`（判定 1 箇所） | 9 本・変異 5/5 赤 |
| 2 D1〜D3・D5 | `ailine_core/pdf_grid.py`（部首だけ正規化・空白は残す・描画の逆算・`2頁12行3列`） | `test_pdf_grid.py` |
| 3 D6・D7 | `form_read.read_form_grid`（Excel と PDF で同じ口）・`read_pdf_book`・`NoTextLayer` | 同上 |
| 4 D9 | `classify_folder_contents(also=(".pdf",))`（forms だけ）・`cmd_forms` の分岐 | e2e 3 本 |
| 5 D10 | `mk_pdf_corpus.py`・`score_pdf.py`（台帳 `PDF_CANNOT_CARRY`・上限 6・`--self-test`） | 台帳の番人 |

変異 6 通り（空白を捨てる／NFKC を全体にかける／郵便番号も数にする／テキスト層なしを黙って通す／
forms が .pdf を拾わない／番地から頁を落とす）すべて赤。

### 6.1 実測 ── 製品の経路（`read_pdf_book`）で 87 冊

```
正      146  (86.9%)   ★ 探針の 142 より上がった（下の T34）
誤報      0            ★ 台帳の外では 0
安全     16 ／ 不親切 4 ／ 誤区分 2
台帳      3   ← PDF が持ち得ない情報（T21 数値 vs 文字・T23 丸め・T33 #REF!）
```

★ **台帳の番人が初日に働いた**: T34（全角の数字 `１，３２０，０００`）を当初「PDF の限界」として台帳に
載せたが、**部首だけを正規化する**形にしたら全角の数字が文字のまま残り、Excel と同じ
「金額の欄が文字」の拒否ができて**正**になった。`--self-test` が「正なのに台帳に載っている」と鳴らし、
理由ごと外した。★ 台帳は「諦めた印」ではなく「いま外れている理由の宣言」── 直れば消える。

### 6.2 残る外れ 22 件は全部「格子の忠実さ」（直せる側）

```
B03〜B06  明細の金額の合計（15,000）と小計（24,000）が合わない  ← 明細の列の割り当て
B19〜B24  請求額の欄も帯の合計も見つからない                     ← 同じ骨（6 冊）
R01/R02   同上（5 頁の冊 ── 請求書のページ選びは効いている）
```

★ 誤報でなく**安全**（空欄＋理由）に落ちているのが設計どおり ── 格子が甘くても嘘はつかない。
次のサイクルは**明細表の列の割り当て**（設計 §1.3(b)）。閾値を足すのではなく、
見出し行の x 位置を列の定義にする形を測ってから決める。

### 6.3 検体

`tests/fixtures/forms/あかね商事_2026-08.{xlsx,pdf}` ── 自分の内容を LibreOffice で PDF にしたもの
（第三者の雛形は repo に入れない）。空白つきの帯（`合　計`）・`¥33,000-`・請求日と番号の同居を含む。
テキスト層の無い PDF は依存なしの最小 PDF をテストが手で書く（`empty_page_pdf`）。


## §7 格子の忠実さ・1 サイクル目（2026-09-12・同日）

§6.2 の 22 件を座標まで降りて読んだら、原因が 2 つに割れた。

### 7.1 採ったもの（測って +6・誤報 0）

| 何 | 実測 | 直し |
|---|---|---|
| 行の束ね方 | `¥ -`（高さ 24pt）は同じ行のラベル（14pt）より top が 5pt 上・bottom が 5pt 下。**中心は 0.1pt 差** | 行は top でなく**垂直の中心**で束ねる |
| 会計書式の 0 | 実物 construction_bill は 0 を `¥                  -` と描く | `¥ -` → 0 ＋ 書式（裸の `-` は文字のまま） |

```
正 146 → 152 (90.5%)   誤報 0   B19〜B24（6 冊）が 安全 → 正
```

### 7.2 却下したもの（測って −12）

B03〜B06 は明細の列がずれて単価を足していた。座標で見ると、`単価`(436) と `金額`(532) の間を
**発行者の住所ブロック**（東京都… 441・TEL 452・E-Mail 470・担当 473・FAX 474・フッタ 496）が
5〜36pt 刻みで埋め、列の鎖を作っていた ── 表と無関係の散文が列の定義に混ざる。

変異「列は**数が入っている行**だけから定義し、残りは最寄りの列へ」を製品を触らずに測った:

```
正 152 → 140（−12）   宛先が 12 冊落ちた
```

数の無い領域（宛先ブロック）が列を失い、敬称と名前が別の列に割れて `_nearest_text_above`
（同じ列の直上）が外れる。**表に効く規則は散文を壊す。** 却下。

★ 次の候補（未測定・発火条件: 次のサイクル）: 列を「ページ全体で 1 本の定義」にせず、
**明細の見出し行より下と上で別に切る**（表の領域と散文の領域を分ける）。
閾値を足すのではなく領域を分ける方向 ── ただし領域の境目を誰が決めるかを先に測る。

### 7.3 見えたまま残す 3 件（T17・T18・T24）

Excel では**食い違いを見つけて 割** にする冊が、PDF では 単 になる（値は合っている）。
格子が甘くて 3 つ目の口（小計＋消費税）を組めていない ── 一番強い番人が黙る形。§7.2 と同根。


## §8 設計の前に測ったら、設計する対象が変わった（2026-09-12・同日）

§7.2（列の領域を分ける）を設計するつもりで前提を測ったら、**別の、もっと大きいものが出た**。

### 8.1 測定の鎖

| 測ったこと | 出た答え |
|---|---|
| 明細の見出しは PDF の格子から見つかるか | 42/85 ── ★ **Excel でも 42** で、PDF の欠陥ではなかった |
| ではなぜ見つからないのか | 見出しが `品番•品名` で、`•` は **U+2022 BULLET**（一覧は `・` U+30FB）。実物のベンダー雛形に 10 セル・検体に 31 セル。**NFKC でも寄らない** |
| 畳むと | 見出し 42 → 72。**出す値は 1 つも変わらない**（`norm` は照合専用と実測） |
| ★ しかし score は | Excel 171 → **168**（誤区分 3）／PDF 152 → **138** |

### 8.2 ★★ 3 冊が正しかったのは、一文字のバグのおかげだった

Excel の誤区分 3 冊（T28 備考の再掲・T38 通貨 USD・X03 見積書 ESTIMATE）は、
器官の初版（`34b3e90`）が「開いていない第三の口」として**既に名指ししていた冊**そのもの。
明細と一致しても、それは**明細という 1 つの口**を掃いただけだ。

★ **一文字の取りこぼしが掃き出し自体を壊していたから、この 3 冊は over-claim せずに済んでいた**
── 番人が間違った理由で効いていた。いちばん脆い形。だから G1 と G3 は**一緒に**出す。

さらに T15（値引き行 −5,000 の陰性対照）は `値引` が帯の開始語に入っていたせいで明細から落ち、
偽の食い違いを出していた。**一文字が検出を隠し、検出が罠を隠していた。**

### 8.3 掃き出していない口は「語の有無」では測れない（実測）

| 測り方 | 捕まる | 偽陽性 |
|---|---|---|
| 語の有無（見積/納品/領収） | 48 冊 | **29** ── misoca のフッタに「無料のクラウド見積・納品・請求書サービス」 |
| 語の有無（備考） | 36 冊 | **25** ── 備考欄はどの請求書にもある |
| 大きい数が在る | 24 冊 | **22** ── 税率 0.1・口座番号・請求番号を拾う |
| **構造**: 円でない通貨の印（`元` は『請求元』に当たるので除く） | T38 | **0** |
| **構造**: **短いセル**が帳票名を名乗る | X01・X03 | **0** |
| **構造**: 合計のラベルと金額が同じ文章に同居し、値が本体と違う | T28 | **0** |

### 8.4 入れたもの（G1〜G3 は一緒に・実測で確かめて）

- **G1** `norm()` が中黒を 1 つに寄せる（`• · ･ ‧ ∙ ⁃ → ・`）── 閉じた**文字クラス**
- **G2** `値引/値引き` を帯の開始語から外す（帯は 小計/合計/消費税 で開く）
- **G3** `unswept_mouths(grid, total)` ── 在れば**裏取り済みを名乗らない**（値は出す・名指しで開示）
- **畳んだ**: D4 の `copies_indistinguishable`（真偽値）を `Record.unconfirmable`（**理由の並び**）へ。
  同じ日に二つ目の理由が出たので、三つ目（CSV・OCR…）で真偽値を足す形をやめた

### 8.5 ★★ 出す前に「偽の疑い」を測って、もう 1 つ足した

G1〜G3 を入れると PDF が 152 → 138 に落ちた。全部 安全（空欄＋理由）だが、中身を測ったら:

```
PDF で出た「明細と帯が合いません」39 件のうち
  ★ 偽の疑い（Excel では裏が取れて正しい値）  34 件
    本物（Excel でも食い違う）                 5 件
```

**87% が偽。** 「空欄は誤値より安い」は「**偽の疑いも安い**」を意味しない ── オオカミ少年は
この製品がいちばん避けるものだ（自分がその番人を売っている）。

→ **格子が自分で申告する**: `Grid.columns_reconstructed`。Excel の列は与えられたもの、
PDF の列は x 座標からの推定。推定の上で表を歩く側（`detail_amount_sum`）は**降りる**。

```
PDF  正 138 → 163 (97.0%)   誤報 0   安全 0    ← G サイクル前の 152 も超えた
Excel 171/171・入れ子 18/25・束 243/255（不変）
```

### 8.6 残った 5 件（見えたまま追いかける）

| 冊 | 何 | どちら |
|---|---|---|
| T17・T18・T19・T24 | 値は合っているが「この冊は壊れている」の通知が出ない | ★ 直せる（§7.2 の列）── 台帳に入れない |
| T32 | 答えが「シート」を名指し。PDF では「頁」と言うのが正しい | 台帳へ（答えが Excel の語彙） |

★ 列が信頼できるようになったら `columns_reconstructed` を下げ、**偽の疑いが 0 であることを測ってから**
明細の掃き出しを PDF でも走らせる。順序を逆にしない。


## §9 三社目のレイアウトが誤報を 1 つ出した（2026-09-12・同日）

Wondershare の別レイアウト（bluegreen）を製品の経路にかけたら、**誤報**が出た:

```
請求番号  単 '請　求　書'      ← 帳票のタイトルを請求番号として出した
```

### 9.1 座標を見たら、閾値では分けられない形だった

```
発行日：      中= 64.7  高=10.0
請 求 書      中= 79.3  高=43.7   ← 57.4〜101.1 と、小さい 2 行にまたがる
請求書番号：   中= 79.6  高=10.0   ← タイトルの中心と 0.3pt 差
```

★ §7.1 で「行は top でなく**垂直の中心**で束ねる」に変えたのは、`¥ -`（大きい金額）が
ラベルの行に**入るべき**だったから。今回はその逆で、大きいタイトルが隣の行に吸い込まれた。
**高さの閾値では両方を満たせない。**

### 9.2 違いは構造だった ── またぐ行の数

| | またぐ小さい行 | 正しい扱い |
|---|---|---|
| `¥ -`（大きい金額） | **1 つ**（ご請求金額の行） | その行に入る |
| `請 求 書`（タイトル） | **2 つ**（発行日・請求書番号） | どの行にも属さない（見出し） |

→ **またぐ行が 1 つなら同じ行・2 つ以上なら自分だけの行**。閾値を増やさずに両方が立つ。

実測: bluegreen の 請求番号 は **無**（誤報が消えた）／87 冊は 163/167・誤報 0 のまま／
変異 4 通り（見出しを見分けない・またぐ行 1 つでも見出しにする・格子が申告しない・
申告を無視して表を歩く）すべて赤。

### 9.3 ★ 2 ページの雛形は「決められない」に倒れる（設計どおり）

Adobe の 4 レイアウト（1 頁目が空の雛形・2 頁目が記入例）を製品の経路にかけると、
**全項目が空欄**になる ── 「この 1 冊に請求書らしいページが 2 枚あります」。
探針では印の多い頁を選んでいたが、製品は**選ばない**（D6）。同じ 1 冊に請求書が 2 枚あるとき
どちらか決められないのは Excel 側と同じ線で、雛形＋記入例という配り方の PDF では
そう出るのが正しい。★ 実務で 2 頁の請求書（明細の続き）が来た時の扱いは**まだ設計していない**
（§2 D6 の発火条件つき保留）。


## §10 A1（明細の列）の測定記録 ── 半分決まって、半分は発火条件つき（2026-09-12）

原因は 4 層に割れた。1 つ潰すごとに次が見えた:

| # | 原因 | 処置（変異で測定） | 偽の食い違い（本物 4〜5） |
|---|---|---|---|
| 0 | 現状（ページ全体で 1 本の列） | ── | **34** |
| 1 | 発行者の住所ブロックが表の列を鎖でつなぐ | 表の領域と散文で列を別に定義 | 13、ただし散文で 5 件壊れる → 却下 |
| 2 | ★ **衝突の逃がし方** ── 同じ行の 2 語（`1` と `個`）が同じ列に落ちると `c += 1` で隣の列の席を奪い、単価が金額の列へずれる | 衝突した 2 語の**間**で列を割る（閾値を増やさない） | **3〜4**・散文 74/74 ○ |
| 3 | 値引きが会計書式の負数 `(¥5,000)` | 括弧の負数を −5000 に逆算 | T15 が直る ○ |
| 4 | 見出し `金額` は列の中央・値は右揃えで中心が 37〜55pt ずれ、最寄りで外れる（T21 は単価の方が近い） | 見出しを下の列へ **x の順番**で対応 | 純度 44→49 だが偽 3→**6** ── 見出しの無い**単位列**（`個`）が対応を 1 つ狂わせる |

★ 確かなもの: 2 と 3。★ 4 は幾何だけでは決まらない ── `個` が単位だと知る手がかりは「列の中身が
文字か数か」しかない。**発火条件**: その手がかりを対応付けに使う案を測る時（偽が 0 になるまで
掃き出しは降りたまま。2・3 を先に入れても 5 項目の点数は動かない ── 確認済み）。

## §11 A2 ── 明細の見出しが見つからない残り 13 冊（Excel）

11 冊が同じ形だった: `商品コード | 品目 | 数量 | 単位 | 単価 | **合計**`（実物の骨 spread_1/3/5）。
行ごとの金額の列が **合計** と名乗っていて、`_DETAIL_HEAD_AMOUNT`（金額／金額（税抜）／小計）に無い。

足す前に測った（G1 の時に「掃き出しが動くと over-claim が出る」を踏んだので）:

| | 前 | 後 |
|---|---|---|
| 見出しが見つかる | 72 | **84** |
| 87 冊の score | 171/171 | 171/171 |
| 区分 | 確 52 | 確 **62**（単 → 確 10 冊・明細の合計が小計と一致） |
| 誤区分（over-claim） | 0 | **0** |
| PDF | 163/167 | 163/167 |

★ 物差しの穴を 1 つ踏んだ: 純度の採点が「不純 1 → 12」と鳴ったが、中身は**雛形の空行の 0**
（V24〜V39 = 0）を答えの明細と比べていただけで、器官は正しく 9000・6000 を足していた。
**赤が出たら製品を触る前に 1 冊を手で解く** ── 同日 3 回目。
残り 2 冊は 明細 0 行（表が無い）と、答えの品目が見つからない 1 冊。


## §12 ソフトが生成した PDF で 5/5（2026-09-12・Namakoo 提供）

探していた「会計ソフトが吐いた記入済み PDF」に最も近い検体が入った
（`tests/fixtures/forms/ソフト発行_請求書.pdf`・フォントが Liberation-Sans ＋ Noto-Sans-CJK-JP で
Producer が空 ＝ Excel で作って印刷したものではない・社名は合成）。

```
宛先 単 株式会社ミライ商事 ／ 請求元 単 株式会社テックイノベーション ／ 請求額 単 407000
請求日 単 2026-09-12 ／ 請求番号 単 INV-202609-001        ── 5/5・誤報 0・予測どおり
```

★ 探した結果の記録: 各社（マネーフォワード 80 種・弥生/Misoca・MakeLeaps）が配っているのは
**`.xlsx` の空テンプレート**で、**自社の出力 PDF は公開していない**。公的機関の記載例
（国税庁 Q&A 問54）は本物の PDF だが「Q&A 文書の中の図」で、`1 冊 = 1 請求書` の前提に合わず
製品は正しく 割（請求書らしい頁が 2 枚）で断る。**ソフト発行の PDF は買うか作るしかない。**

### 12.1 却下: 明細の見出しを「一覧の語で始まる」に緩める

この検体の見出しは `品名・明細` で、一覧（品番・品名/品名/品目/内容/摘要/件名/作業内容）と完全一致しない。
緩める案を測った:

| 測ったもの | 結果 |
|---|---|
| 見出しが見つかる冊 | 検体 87: 84 → 84（増えない）／PDF: 2 → 3 |
| ★ **見出し行が別の場所へ移った冊** | **18 冊**（検体 11・束 7）── 正しい `D21/V21` を捨てて `A19/O19` を拾う |
| 得 | この 1 通だけ。しかも PDF は掃き出しを降りているので**点数は 1 も動かない** |

★ セル数（`合計` で始まるセル 389 件・中身に `合計金額調整費` や備考の文章）は**指標にならなかった** ──
見出し行は「品名側と金額側が同じ行」が条件なので、セル数では危険が測れない。
**見出し行が動くかを測って**却下した。番人に記録（`test_the_detail_header_of_that_pdf_is_not_found_and_that_is_recorded`）。
★ 発火条件: `品名・明細` 型が別の出所で 2 件目出た時、または PDF の列が直って掃き出しを再開する時。

### 12.2 ついでに見えた穴（未処置）

国税庁の記載例に **`㈱`（U+3231 の合字）** と半角カナ `ｷｯﾁﾝﾍﾟｰﾊﾟｰ` が実在した。法人格の一覧は
`株式会社` 系だけなので `㈱` は当たらない。NFKC は `㈱ → (株)` に寄せるので、中黒と同じ
**閉じた文字クラス**の話 ── 測れる穴。発火条件: 次に法人格まわりへ手を入れる回。
