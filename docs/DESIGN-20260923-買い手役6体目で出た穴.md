# 買い手役 6 体目で出た穴（2026-09-22 実施）

★ この回から **docs/盲検の記録様式.md** の 5 欄で書く（1 回目）。

- `分母:` **5,225 件**（`5d087df` 時点で緑だった試験数・機械が書いた `TOTAL_TESTS`）
- `役柄:` 税理士事務所の担当者（プログラマではない・Excel は毎日使うがマクロは書けない）
- `渡したもの:` `README.md` **だけ**（`src/` `tests/` `bench/` `docs/` は禁止）。
  **GUI は使わせていない**（今日の直しがほぼ全部 CLI の画面なので、変えた面を真っ直ぐ測る）。
  冊は**新しく 5 つ作った**（過去回の冊は使わない ── 直した所を踏みに行くだけになる）。
  過去の前例・期待値・直した箇所は 1 つも渡していない。

---

## 所見

| # | 区分 | 所見 | 再現 | 致命 |
|---|---|---|---|---|
| 1 | 欠陥 | 「B1からF1の結合を解除して」が `操作:セル結合` と読まれ `✓ 機械検証済み`。依頼と真逆の操作に ✓ | 再現 | 致命 |
| 2 | 欠陥 | 同じ言い方を結合していない範囲に出すと**本当に結合し**、見出し 2 つを空にして `△ 検算が合いました` | 再現 | 致命 |
| 3 | 欠陥 | #2 で作った冊を `export-csv` で読むと見出しが**残っている**。2 つの読み手が同じ冊について違うことを言う | 未再現 | 致命 |
| 4 | 欠陥 | 行番号を指した 1 セル書換が、毎回でたらめな ⚠ を出して ✓ を取り下げる | 未再現 | ─ |
| 5 | 欠陥 | 「取引先が abc商事 の行の取引先を ＡＢＣ商事 に書き換えて」が**行追加**に化ける | 未再現 | ─ |
| 6 | 欠陥 | `--column` が 2 冊に同時に掛かる。道具が案内した通りに打つと落ちる | 再現 | 致命 |
| 7 | 欠陥 | 「取引先をキーに」と書いてあるのに「『取引先をキーに』と書け」と言う | 未再現 | ─ |
| 8 | 欠陥 | 2 冊照合が「式のままで計算結果が入っていない」と言うが、`export-csv` では値が入っていた | 未再現 | ─ |
| 9 | 欠陥 | `ailine history` の台帳パスが文字化け（`6菴鍋岼`） | 再現 | ─ |
| 10 | 摩擦 | 結合セルがあると集計もピボットも空振りし、道具は「結合を解除してから」と案内するが**解除する手段が無い** | 再現 | 致命 |
| 11 | 摩擦 | 空行で走査が止まる。警告はコンソールだけで**出力ファイルには残らない** | 引用で明白 | 致命 |
| 12 | 摩擦 | 「増減の列に入れて」が既存の空列を使わず新列を作る。正解の言い方に 3 回かかる | 引用で明白 | ─ |
| 13 | 摩擦 | 2 回読みが割れた時の選択肢に、どちらを選ぶ判断材料が無い | 引用で明白 | ─ |
| 14 | 摩擦 | ⚠ が見出しの回でも本文に「検算しました」と書いてある。どちらが結論か画面だけでは決まらない | 引用で明白 | ─ |
| 15 | 摩擦 | `run -h` が開発者向け（`--values`(W3 Part3) / `--accept-loss`(往復忠実度ゲート) など） | 引用で明白 | ─ |
| 16 | 摩擦 | 自由生成について 3 箇所が食い違う（ヘルプ「提供していません」／`ops`「AI が直接生成することがあります」／2 回読み画面「自由生成へ退避する」） | 引用で明白 | ─ |
| 17 | 摩擦 | `⚠ 機械保証はありません` でも終了コードが 0 | 引用で明白 | ─ |
| 18 | 要望 | 空欄で絞り込めない ──**未入金一覧が作れない**（月末で一番やりたい作業） | 再現 | ─ |
| 19 | 要望 | 別ファイル間の転記ができない。README「主な利用場面」に在ると書いてあり 4 回無駄打ちした | 再現 | ─ |
| 20 | 要望 | 表記ゆれの一括統一が無い | 引用で明白 | ─ |

**集計（★ 数は機械が読んだ ── `tests/blind_rate_core.py`）**: 所見 20（欠陥 9 / 摩擦 8 / 要望 3）・未再現 5・**致命 6**（#1 #2 #3 #6 #10 #11）
★ 私は手で数えて「致命 5」と書いた ── **番人が数え直して違いが出た**。今日 4 回目の手勘定の誤り
（`**` 3→31 件／履歴の汚染 22%→96%／事後条件 3→14 箇所／ここ）。**数は機械から取る。**
★ 誤報率 **25%（5/20）** ── 外の目も測定器なので必ず併記する。

## 値段

**0 円。買わない。月額も買い切りも無し。同僚にも勧めない。**

> 「この道具の売り文句は『できたと言わないこと』ですが、**私が 1 時間で
> 『できていないのに ✓ と言った』場面を引き当てました**」

★ 条件つきの値は付いた ──「`split` + `verify` の部分だけなら**買い切り 5,000〜10,000 円**の
価値を今日感じた。ただしそれは『日本語で Excel を操作する道具』ではなく
**『配って数える道具』**で、**別の商品**だと思います」。

★★ 1 体目（09-15）も `split` のためだけに **3,000 円**と言っている。
**別々の買い手が 2 人、7 日あけて、こちらから何も示唆せずに `split` に値を付けた**（しかも上がった）。
需要の記録（上位 5 件中 4 件が複数ファイル）とも向きが合う。

## 良かった所（公平に・買い手が挙げた 8 件）

`doctor`（9 項目が全部 ✓ で最初の 30 秒で動く状態だと確信できた）／**`split`**（「今日いちばん
気持ちよかった」「`✓ 部分の和 ＝ 全体` は所長が見ても意味が分かる 1 行」）／上書きの関所／
`export-csv` の数式の開示（勝手に 0 を入れない）／空行で止まった時の申告／`accounts` の
「名前が近い列」提示／`undo` が一発で効く（残り回数まで出る）／**断る時に断る**
（「それらしい結果を作って見せなかったのは、他の AI ツールより明確に良い」）。

## 処置（2026-09-22〜23）

| # | 処置 |
|---|---|
| 1・2 | 依頼が「その op の概念語 + 打ち消しの語」を**隣接**で含むなら断る。概念語は宣言（`OP_LABELS`）から引く。配線は `resolve_dsl_step_args` 1 箇所 |
| 6 | `--column` を**冊ごとの手がかり**にし、役割ごとに**候補の並び**を持つ。別名は 1 つも足していない（語彙でなく仕組みで解いた） |
| 6 の残り（09-23） | ★ **半分しか閉じていなかった** ── 導線の台帳を歩いて見つけた（歩き手の最初の 1 歩・2 回とも再現）。仕組みは入ったのに、**案内がそれを教えていなかった**（1 回目は今回の冊の分だけ「もう一度」と言い、従うと過去の冊で落ち、2 回目の画面に次の手が無い）。→ 1 回目で**全部の冊の `--column` を 1 行に**まとめる・同じ見出しが 2 列ある冊は「選べない」と正直に言う（「外せ ⇄ 付けろ」の往復を再現して止めた）（`25d2ef6`） |
| 9 | 置き場の名が壊れて見えるなら**直さずに言う**。★ 実体は「実際にその名前でフォルダを作り、履歴も **undo の世代も**そちらへ書く」で、表示の問題ではなかった |
| 10 の真因 | **「表の左端は必ず埋まっている」**という前提を 3 層から外した（Basic の列 7 箇所を `HeaderLastCol` に畳む・Python の列走査・定数を両言語で一致させる番人） |
| ★ 別件 | 「見出しを太字に」が `--header-row 3` でも 1 行目に掛かり ⚠ exit 0 → **機械が見出し行を決める**（出所を画面に出す） |

★ **3 は発生源（#2）が消えたので未確認のまま残す** ── 消えた原因の下流を「直った」と書かない。

★★ 2026-09-23 に**7 体目の前の掃除**として、導線の台帳 40 件（「こう打てば進める」と言っている所）を
全部歩いた（サブエージェント 4 体が歩き方を書き、機械が歩く）。結果: path_fails 1（上の 6 の残り）・
往復の罠 1（同じ家系）・**届かないコード 1**（accounts の冊の検算の案内が 2 か所に違う文面で在り、
丁寧な方は一度も画面に出ない ── `e04af59` で 1 つに畳んだ）・残りは道が通った。
★ 朝に Namakoo から「盲検で指摘されそうな欠陥は事前に見つけるのは難しい？」と聞かれ、
「既に在る道具を向ける」と答えた。その実行で、**致命がまだ閉じていなかったこと**が見えた。

## ★ この回で分かった、こちらの測り方の穴

- **#10 の「心当たり」が嘘だった** ──「結合を解除してから」と案内しながら、解除しても直らず、
  そもそも解除する手段が無い。**導線が嘘なら無い方がまし**の族。
- 私は一度「真因は 1 列目が空で走査が止まること」と報告した。**誤りだった**
  （`header_row=1` で測ったから出た数字）。★ 同じ日に 3 度目の「測定器の設定を確かめずに数字を読む」。
- 直した門が**正しい依頼を 2 件止めた**（「合計行を**除いて**並べ替えて」）。
  ★ 素の環境では出ず、**実機の番人だけが捕まえた**。
- `walk_refusals`（断りが示した道を実際に歩く道具）は**既に在る**のに、
  台帳に `accounts` が 1 件も載っていなかった。#6・#7 はその `path_fails` そのもの。
