# 盲検 買い手役 5 体目で出た穴（2026-09-20・雑貨の小売店の店長）

役は**雑貨を扱う小売店の店長**（従業員 8 人・2 店舗・40 代）。過去 4 体（総務経理／営業事務／
購買／記帳代行）と重ならない仕事を選んだ。README だけを渡し、**自分でファイルを作らせた**。

★ 今日直したこと（御中の取り違え・`--column`・実物の見え方・式の列を自分で埋める）は
  **一切伝えていない**。効いたかどうかは、向こうが踏むかどうかでしか分からない。

★ 実測: 買い手役の消費は **107,883 トークン / 道具の呼び出し 48 回 / 12 分**。
  ★ 俺の事前の見積もりは「数十万トークンの桁・30 分〜1 時間」── **3〜5 倍高く外した**。
    以後はこの実測で話す（`prepare`/`freeze` で依頼文が残るようになったのもこの回から）。

---

## ★★ この回いちばん大きいこと ── 嘘の到達が 0 件

> **今回、間違った答えに ✓ が付いた例は 1 件もありませんでした。**
> 数字は全部自分で検算しましたが、全部合っていました。

1〜4 体目は**全部**「静かに間違う」を踏んでいる。**5 体目で初めて 0 件になった。**

合格線がいちばん重く扱う所も効いた:

> 「税込み」と言ったのに税を掛けていないことを、自分から白状して止まったこと。
> **あれで、この道具の言う ✓ は信じていいんだなと思いました。そこはよその道具にない値打ちだと思います。**

いちばん効いた機能は **2 冊の突き合わせ**。仕込んだ「2,000 円多い請求」も
「頼んでいない商品の請求」も両方拾った ──「これが毎月いちばん時間を食っている作業です」。

---

## 出た穴と、根（★ 読んで確かめた）

| # | 穴 | 根 | 状態 |
|---|---|---|---|
| ① | **関所で止まった run が `.out.xlsx` を黙って残す**（しかも作業は完了していた） | 自由生成の関所が `return e.exit_code` で `_finish_run` を素通り | 未処置 |
| ② | 空のファイルに `--header-row 3` を勧める（2 回無駄打ち） | 「**決まらない**」（材料はあるが一意でない＝3）と「**無い**」（材料がゼロ＝9）を同じ出口に流していた ── フォルダの階層では 09-13 に処置済み（**片配線**） | **処置済**（09-21） |
| ③ | 「メールで送って」→「**メール送信の詳細**（例: けい線を引いて…）を教えてください」 | `WE_DO_NOT_DO` にメールが無い | 未処置 |
| ④ | 無い列（チェック列）を頼むと**別の列（差額）**を選んで上書きしようとする | 反証の材料を**実在物との照合だけ**で拾っていた ── 実在しない列名は見えず ②（無言）に落ちて ✓ が出る | **処置済**（09-21） |
| ⑤ | 断りの例文が、**そのエラーの原因そのもの** | 例文の「引く側」に参照表の 1 列目をそのまま入れており、それが転記先と同名だった | 未処置 |

### ① 黙って残る `.out.xlsx` ── **同じ家系の 3 件目**

買い手の言葉:

> 私は置いていません。そしてその `商品リスト.out.xlsx` を開いたら、**私が頼んだ並べ替えが
> ちゃんと済んだ状態で入っていました。**… 素人にファイルを消させるのは怖いです。
> **作業はできていたのに、それを教えてくれない**のが一番困りました。

★★ この形は **2 度直してある**:
- 2026-08-26 `_finish_failed_apply`（反映に**失敗**した経路）
- 2026-09-16 `_finish_gated`（**上書き**の関所）

そして今回は **自由生成の関所**（exit 8）が残っていた。`run_freeform_plan_step` の
`except _FreeformGateAbort as e: return e.exit_code` が `_finish_run` を通らない。
★ 3 度とも「片方だけ直すと必ずこうなる」と書いてあるのに、3 度目が出た ──
**出口の一覧を宣言から導いていない**のが本当の根。

### ③ メールの断り ── **今日の直しが作った画面**

```
？ メール送信の詳細（例: 「けい線を引いて」／「列幅を自動調整して」）を教えてください
```

聞き返しの「（例: …）」に実測で通る例を差し込む器官（2026-09-20 に配線した）が、
**メールの質問に罫線の例を入れた**。買い手は「メール送信はできるのか」と誤解している。

★★ ところが `intent.WE_DO_NOT_DO` には、まさにこの形の反省が書いてある:

> ★ 2026-09-13（買い手役 2 回とも）: 「画像を挿入して」が 1 通目で「位置やサイズ／
> ファイルパスを教えてください」と**できると読める**断りになり… 最初から名指しで断る

**器官は在って、メールが表に無いだけ。**実際「画像を挿入して」は今回もきれいに断れている。

### ⑤ 例文が誤りを再生産する ── `path_fails` そのもの

```
止めた理由: 転記のキー列と対象列がどちらも『商品名』になっています。
…（例:「商品名で引いて商品名を転記して」）
```

例文の「引く側」は**参照表の 1 列目**をそのまま入れている。買い手の発注記録は 1 列目が
『商品名』で、転記先も『商品名』── だから例文が**そのまま同じエラーを再生産する**。

★ これは導通の盤がいちばん重く扱う `path_fails`。**盤に載っていない断り**だった
（台帳 `refusal_register.json` にこの断りが無い）。

---

### ④ 無い列を名乗ると、別の列に書いて `✓` が出る ── 「出ないことは信号でない」

**根**: 仕分けは 3 段階（①照合できた／②無言／③矛盾）で、③ なら `✓` を出さないと**既に決まっていた**。
器官は在った。ところが反証の材料を拾う `task_designators` は**実在物との照合だけ**で拾う設計で、
`チェック列` は実在しないので**見えない** ── 誰も拾わなかった語として残らず、②（無言）に落ちていた。

**効かせる範囲を絞った理由**（実装前に数えた）: 「◯◯列」は 2 通りに使われる。

| 使い方 | 例 |
|---|---|
| 既存の列を名指す | 「在庫列に「◎」を付けて」 |
| **これから作る列を説明する** | 「売上から原価を**引いた列**を作って」 |

検体に後者が 16 件あった。素朴に当てれば全部誤爆する。
だから **既存列にだけ書く op** の時だけ当て、それがどれかは `OP_WRITE_TARGET` の宣言から導く
（`SET_WHERE` / `SET_COLUMN_VALUE` の 2 つ。手で並べない）。

**実測**（凍結した盲検セット ── 冊と依頼が対で保存されている唯一の分母）:

| 依頼 | `確認` 列 | 結果 | 処置前の判定 |
|---|---|---|---|
| 確認列に「◎」を付けて | 在る | 素通り | `✓` のまま |
| チェック列に「◎」を付けて | 無い | **止まる** | `ok=True verdict=verified` ← **嘘の `✓`** |
| チェック列に「◎」を付けて（別冊） | 無い | **止まる** | 既に `ok=False` |

誤爆 0 件・事故の捕捉 1 件。全試験 5003 件は緑（赤は公開面の記録 1 件のみ＝意図した署名変更）。

★★ **測定器の誤り**（記録として残す）: 最初は `~/.ailine/history.jsonl`（18,024 行）を分母にして
「752 run 中 7 件が止まる」と出た。**これは無効だった** ── 記録の `book` は**パス**であって
冊の中身ではなく、demo 冊は run のたびに書き換わっている。同じ依頼が H 列と I 列の両方に
書いた記録があり、列構成が違う時点の run を**今の冊**で判定していた。
★ 本番が残す記録でも、**参照先が可変なら分母にならない**。

★ **残した所見（未処置）── 人に見せる文言が粗い**:
語の切り出しが前を食うことがあり、画面にこう出る。

    「500以上ならチェック列」   ← 依頼「金額が500以上ならチェック列に…」
    「対して確認列」             ← 依頼「…の行に対して確認列に…」

嘘ではない（依頼文にその文字列は実在する）が、買い手に見せる文字として粗い。
直す方向が**除外文字を足す**しかなく、それは「番人を字面で書かない」に反する上、
「て」を切ると「手当て列」が「当て列」になって**悪化する**。
★ 言葉の最終調整でまとめて扱う（`feedback_ailine_wording_is_provisional`）。

### ② 空の冊に `--header-row` を勧める ── 09-13 に塞いだ穴の、別の階層

**買い手が打った 2 発**（`argv.jsonl` にそのまま残っている）:

    09:56:07  run 空.xlsx 金額の合計を出して
    09:56:27  run 空.xlsx 金額の合計を出して --header-row 1

| 打鍵 | 出た返事 | 実際の原因 |
|---|---|---|
| 1 発目 | 見出しが何行目か分かりません → `--header-row 3` を勧める | 冊が空 |
| 2 発目 | **語彙外**（`failure_kind`） | 冊が空 |

★★ **2 発とも原因は同じなのに、画面が指した先は 2 回とも違った。** どちらの指摘に
従っても直らない ── だから 2 回とも無駄打ちになった。

**根**: `resolve_header_rows` は `detect_header_row` の `confident=False` を
**理由を問わず**同じ CLARIFY に流していた。理由は 2 通り以上ある ──
「候補が複数で決められない」（`--header-row` は正しい導線）と「行が 1 つも無い」
（何行目を指定しても永遠に解決しない）。

★★ **これは新しい穴ではない。** `multifile.nothing_to_read` の docstring が
09-13（買い手役 3 体のうち 2 体・空のフォルダ）にこう書いている ──
「**『文書が無い』は 9**（ENGINEERING.md の表）」。同じ判断がシートの階層に
配線されていなかった＝**片配線**。

**処置**: `--header-row` の分岐より**手前**に 1 箇所だけ置いた。そこなら両方の打鍵が
同じ出口に入る（2 箇所に書かない）。判定は既存の器官 `_used_extent`
（中身のあるセルが 1 つも無ければ `(0, 0)`）── 数え直さない。

    × 『Sheet』シートには表がありません（中身のあるセルが 1 つもありません）
      ・`--header-row` では直せません ── 何行目を指定しても、そこに見出しがありません
      → ファイルは作っていません                                        exit 9

★ 副次: 空と分かった時点で止まるので、**翻訳（ollama）を叩かなくなった**。

★ **塞いでも失う能力が無い**ことを実装前に確かめた: 1 セルに書く経路
`resolve_cell_target_from_task` は実表の値を手がかりに位置を決めるので、
手がかりが 0 の冊では元から `None` を返す（`if not rows or not heads`）。

★★ **変異試験が番人自身の穴を見つけた**（記録として残す）: 「読めなかった冊を『空』と
言う」への緩めが**緑のまま通った** ── 検体が `load_workbook` の失敗しか通しておらず、
**開けた後に途中で落ちる**経路を誰も見ていなかった。「見ていないことを根拠にしない」は
両方の落ち方を塞いで初めて守られる。検体を足して 8 変異すべて赤。

## 時間を取られたこと（CONFUSING）

- ~~**README に書いてない機能が、いちばん役に立った**~~ → **処置済（09-21）**。
  ★ 実は**書いてあった**（下の節に例つき）。買い手が見たのは**コマンド表**で、
  そこに行が無かったから「載っていない」と判断した ── 下の「⑤」を参照
- ~~`転記` は参照表が **2 列ちょうど**でないと動かない~~ → **処置済（09-21）**。
  参照表の列を**名前で**読むようにした（実機で 6 列マスタを通した）── 下の「⑥」を参照
- 「重複している行を削除して」と頼んでも**削除されない**（別シートに作る・⚠ では言う）
- 「率:1.0」「事後条件」「語彙外」「DSL 経路」が分からない（⑦と同じ）
- ~~**★ の一行がほぼ毎回出る**~~ → **処置済（09-21）**。**61% → 17%**
  ── 下の「④ 出所追跡」を参照
- ~~**間違った ⚠ で判定が ✓ → △ に落ちた**~~ → **処置済（09-21）**。買い手の推測は当たっていた
  ── 下の「③ 偽の言及」を参照

---

### ③ 偽の ⚠ が正しい `✓` を `△` に落とす ── 知っているのに使っていない

**再現した依頼**（凍結セットの実物の冊）:

    請求シートで請求金額から発注金額を引いた差額の列を作って

    シート … 請求 / 発注 / 商品名・発注金額だけ
    列   … 請求番号 / 仕入先 / 商品名 / 請求金額 / 発注金額 / 差額 / 確認 …

『発注』はこの文中で **列名『発注金額』の内側にしか現れない**。
★ 買い手の推測（「『発注金額』という列名に反応したと思われる」）は**当たっていた**。

**根は 2 つ、どちらも「機械が知っているのに使っていない」形**:

| | 根 | 直し |
|---|---|---|
| ① | 覆う語彙に**列名が入っていなかった** | 覆う候補に実在する列名を足す（規則は不変） |
| ② | 文面が「存在しません**／**変更されていません」と**違う 2 つの事実を畳んでいた** | 原本のシート一覧を見て言い分ける |

★ ① の器官（`drop_names_covered_by_longer`・名前の**位置**で見る）は 08-24 から在って
判定規則も正しかった。**器官は在ったが語彙が届いていなかった** ── 規則を一切変えずに解けた。

★★ ② で**自分の推論を採用しなかった**: `sheet_names_mentioned_in` は実在名しか返さないので
「シートの枝で『存在しない』は構造上起こりえない」と読める。だがその推論に寄りかからず、
**実際に見て**言い分ける形にした。前提が将来崩れても嘘にならない側に置く。
番人にも「本当に無い名前には『存在しません』と言えること」を入れ、片側だけ直った状態を検出する。

**実装前に計算した爆風**（過去に実測で入った検体）:

| 検体 | いま | 直後 |
|---|---|---|
| 08-24 独立して書かれた短い名前は消さない | 売上 / 売上60以上 | 同じ |
| 08-24 長い名前に畳まれる方は消す | 売上60以上 | 同じ |
| 08-28 雛形 ← 雛形_英文 | 雛形 | 同じ |
| **09-20 買い手の事故** | 発注 / 請求 | **請求 のみ** |
| 陰性: シートを本当に名指す | 発注 | 同じ |

**実測**: 全試験 5027 緑・爆風ゼロ。golden は 3 本が文言のみ変更で**助言の増減はゼロ**。

★★ **既存の番人に止められた**（記録として残す）: 新しい試験が本体をパスで読んでいて、
`test_guard_ledger` の「場所の決め打ちは在庫ゼロ」を 1 件増やした。理由は
「分割で実装が動くと黙って空振りする」── **上限を上げずに載せ替えた**
（`product_files()`）。載せ替え後も配線の変異が鳴ることを確認済み。

★ **等価変異を 1 つ見つけた**: 「シート名と同名の列も覆いにする」に観測差がゼロだった。
調べると `drop_names_covered_by_longer` は**より長い**名前でしか畳まないので、同名は
構造上覆えない ── その条件は何も守っていなかったので外した。
**番人が鳴らない＝穴、とは限らない**（鳴らない理由を読むまで分からない）。

### ④ ★ の一行が成功 run の 61% で出る ── 自分の置き土産を理由に迷っていた

**実測**（凍結セッションの解釈行と依頼文＝どちらも run 時点の事実）: **11/18 = 61%**。
発火した回で競合していたシートは『品番の重複除去』『集計』『商品名・発注金額だけ』『検分』
── ★★ **どれも ailine 自身の出力**。自分が散らかしたものを理由に
「どのシートか分かりません」と言っていた。

★★ **同じ病気の 2 度目**。`claim.py` の docstring が既にこう書いている ──
「旧注記は ✓ が出る**全 run で必ず**出ていた。発火率 100% ＝ 情報量ゼロ。
誤爆する警告を批判している設計側が同じ病気にかかっていた」。
100% → 61% に下げたが、**下げた後の率を誰も測っていなかった**。

**なぜ A（自分の出力を数えない）がすぐ作れなかったか**: 既存の
`_looks_like_own_prior_output` は **形（見出しの一致）**でしか判定できず、
docstring 自身が敵対検証（08-20）の結論として「**真の出所追跡は未実装**」
「人の手作りシートと区別できない」と書いている。その上に建てると
**人のシートを自分のものと誤認して、警告が要る回に黙る**。

**建てたもの**（Namakoo 決裁: 真の出所追跡を先に）:

| | |
|---|---|
| 記録① | `record_diff` ── 差分を書く **4 経路を 1 本に畳み**、表示（`changes`）と出所（`made_sheets`）を同じ所で決める |
| 記録② | `record_made_book` ── 冊まるごと作る入口が、作った時に残す |
| 運ぶ | 台帳の固定列挙に追加（★ `out_sha` が写されず関所が到達不能だった前例と同じ項） |
| 読む | `sheets_ailine_made(冊)` ── 記録が無ければ**空** |
| 使う | 「複数シートのブックだけ」の**複数**を、**人が作った枚数**で数える |

★★ **失敗の向きを設計で決めた**: 記録が無い・冊を移した/複製した・台帳が読めない
→ 空 → 「全部 人のもの」→ **注記は出続ける**。黙って消える警告が一番高くつく。

**実測**（対照つき・同じ冊で対にした）:

| 冊の出どころ | 対照（出所なし） | 本番 |
|---|---|---|
| `stack` が丸ごと作った | ★ 出る | **消える** |
| `run` が足したシート | ★ 出る | **消える** |

買い手の並び全体では **61% → 17%**。残る 3 件は請求と発注が**どちらも人のもの**で、
本当にどちらにも転びえた回 ── 注記が仕事をしている回だけが残る。

---

#### この回で拾った 4 つ（記録として残す）

★★ **パスの向きで静かに死んでいた**。`stack` が書いた `C:\…\全店売上.xlsx` と
人が打った `C:/…/全店売上.xlsx` が別物になり、**記録は在るのに一度も当たらなかった**。
怖いのは**挙動に出なかった**こと ── 当たらない＝「人のもの」扱いで注記が出続けるだけ
なので、安全側に倒れて見えなくなっていた。**陽性側を実測しなければ気づけない死に方**。

★★ **番人が初回から穴を 3 つ掴んだ**。`cmd_run_folder`・`cmd_run_match`
（買い手が「いちばん役に立った」と言った **2 冊並べ**の経路）と `cmd_demo` が、
冊を仕上げているのに出所を一切残していなかった。

★★ **既存の番人に 2 度止められた**。①「履歴を書く実装が 2 箇所ある」── 探したら
`run` を通らない道の記録は **09-18（買い手 3 体目 ⑨）で既に 1 本に畳んであり**、
私は 3 本目を作りかけていた。②「場所の決め打ちは在庫ゼロ」（③ の回）。
**どちらも上限を上げずに畳み直した。**

★★ **測定器の誤りを 3 度踏んだ**（同じ形）。凍結された冊は**最終状態**であって
run 時点の状態ではない ── `~/.ailine/history.jsonl` の `book` はパスで中身が動く、
凍結セットの `books/` も最終状態。★ **凍結物を今の状態で測る**癖として記録する。

### ⑤ README ── 番人が緑のまま、買い手は見つけられなかった

**2 冊並べは README に書いてあった**（例つきの節がある）。買い手が見たのは
**コマンド表**で、そこに行が無かったから「載っていない」と判断し、20 分を失った。

★★ そして前に書いた番人 `test_the_two_book_form_is_documented` は
**「README のどこかに在るか」**を見ていて、**緑だった**。

> 在るかどうかでなく、**探す人が見る場所に在るか**が問われていた。

「在っても鳴らない」の人間側。表に行を足し、番人を**「表の中に在ること」**へ締め直した
（表から行を消すと鳴ることを変異で確認）。

### ⑥ 転記 ── 正しい列を計算しておきながら、使っていなかった

買い手の発注記録は **6 列**で、転記は構造的に使えなかった
（「転記は参照表が 2 列ちょうどでないと動きません」）。

**根**: 書き手 `VLookupFromTable` が「参照表の 1 列目＝キー・2 列目＝値」を決め打ちし、
検算も同じ決め打ちで期待値を作る ── **やる側と見る側が同じ思い込みを共有**していて
必ず一致する（恒真）。08-24 の盲検で「単価の列に『果物』が入って ✓」が出た根。

★★ **位置を割り出す式は、警告を出すために既に書かれていた。** 正しい列を計算して
「意図と違うかもしれません」と言うだけで、それで書きに行かなかった ──
**器官は在るが配線が無い**。

| | |
|---|---|
| ヘルパ | `Optional lookupKeyCol, lookupValueCol`（既定 0/1 ＝ 従来の呼び方を壊さない） |
| 生成 | 参照表の見出しから**名前で**引いて渡す |
| 検算 | **ファイルそのもの**から名前で引く（生成は `book_meta` から ── **別の道**で同じ名前に辿り着くので恒真にならない） |
| 警告 | **名前で引けない時だけ**に狭めた（線は残す） |

**実機で通した**（Basic を変えたので、ここを通らないと「直った」と言えない）:

    6 列マスタ（商品/区分/仕入先/単価/備考/更新日）から「単価を転記して」
      → C2:120  C3:80  C4:150   ✓ 機械検証済み

後方互換も実機で測った ── カタログが模型に見せている**5 引数**の呼び方も、
新しい 7 引数も、どちらも正しく走る（`Optional` が効く）。

★ その測定で**治具の誤りを 1 度経由した**: 最初 `Sub DoWork` + `ThisComponent` で
組んで「5 引数は失敗する」と出たが、本番の包みは `Sub Run(oDoc As Object)` だった。
**測定器を先に疑う**（今日 4 度目・ただし信じる前に捕まえた）。

## 値段（★ 過去の前例は渡していない）

**いまの形なら月 0 円。**ただし理由が「動かないから」ではない:

> 動きます。中身も正しかったです。払わない理由は 3 つ ──
> ① コマンドが重い（従業員には渡せない）
> ② 言い直しが何度もある。**私の表は 10 行です。10 行なら手でやったほうが早い**
> ③ 勝手にできる `.out.xlsx` が怖い

**画面が付いて「請求書と発注記録を選ぶだけ」で突合が出るなら月 2,000〜3,000 円。**
同業の店長仲間 3 人に勧める、とも言っている。

★★ ただし本人が明記している ──「上の『払わない』は **GUI を見ずに出した結論**です」。
  ★ 盲検の作法として GUI は使わせていない（Namakoo の決定）。その判断が、
    この回で初めて**値段に直結して**出てきた。

---

## 試せなかったこと（★ ASSUMED を最初に読む）

- **GUI** ── 使うなと指示したため。**購入判断のいちばんの分かれ目がここ**だと本人が言っている
- Excel でファイルを開いたまま実行（ロック検出）── 手段がなく未確認
- `forms` / `split` / `accounts` ── 手元に該当書類が無く未確認
  （★ **PDF の請求書が読めるなら判断は変わる可能性がある**、と本人が言っている）
- `undo` の多段（3 回以上）・`redo`
- **100 行を超える本物の量**（作った表は最大 11 行）── 実務の量での速さと正しさは未測定
- `--overwrite` で実際にデータが消える瞬間（危険の形を見ただけ）
