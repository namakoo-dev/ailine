# 設計: N 冊から決まった項目を集める ── 語のマップは配らず、その場のフォルダから作る

2026-09-10・Namakoo と合意した方針を、当日の実測の上に書く。**まだ実装していない。**

```
決めたこと   語（取引先／宛先／御中…）の対応表を**出荷しない**。
             N 冊を先に全部読み、列の**中身の輪郭**で役割を決め、
             同じ役割に落ちた見出しの語を束ねて、その場で語のマップを作る。
             マップは成果物として人に見せ、直せて、覚えさせられる。
発火         需要地図の上位①「請求書 40 ファイルからの抜き出し」
```

---

## 0. なぜこの形か（当日の測定）

同じ 40 冊の合成検体（`bench/folder_map/mk_invoices.py`・見出しの語 ×4〜5 通り／
飾り行 0〜2 行／列順 4 通り／金額が文字列／合計行あり／赤伝／請求ゼロ／結合セル）で、
4 つの案を**同じ答え**（検体側が持つ `_答え.json`）で採点した。

```
                                       取引先と請求額が両方当たった   うち「推測」
現状（scan / run <folder>）                    1 / 40                   ─
層1 のみ（見出し行を各冊で推定）               2 / 40                   ─
1 冊ずつ輪郭で当てる（語を読まない）          39 / 40                  17
★ 先に 40 冊読んで語のマップを作る            39 / 40                   0
```

★ 当たった数は同じでも、**根拠の質が変わる**。「一意率が低いから多分これ」（推測 17）が
「この語は 40 冊中 10 冊で同じ役割に落ちた」（推測 0）になる。人に見せて頷いてもらえる形。

★ 残る 1 冊は語でも輪郭でもなく、**見出し行の読み違い**（2 行目を 4 行目と読んだ）。
これは「40 冊中 39 冊が 4 列なのに、この冊だけ列がずれている」という**横の証拠**で
気づける位置にある（§3.4）。

### 崩れた読みも記録する（同日）

- 「層1 だけで 1 → 27 件」と口にしたが、実測は **+1**。scan の「26 件が見出し行で落ちる」は
  同時に語も違っていた。分母を確かめずに割合を言った。
- 最初の検体は列順が全冊同じだった。**位置で当たっているのか輪郭で当てているのか分けられない**
  ので、列順をバラしてから測り直した。
- 「抽出」は在ると思っていたが、フォルダの抽出は**行の絞り込み**（同じスキーマの表を条件で絞る）で、
  需要①の「項目を集める」とは**別の操作**だった。名前から中身を推測していた。

---

## 0b. ★★ 実物 16 冊で測ったら、この設計の中心が出番を失った（同日 夕）

Namakoo が実物の請求書雛形を持ってきた（misoca 15 冊＋マネーフォワード 1 冊＝2 ベンダ）。
**§0 の測定は合成 40 冊で、そこで置いた前提のほとんどが実物では成り立っていなかった。**

```
俺が合成で難しくした軸        実物
  シート名がバラける          ★ misoca 15 冊すべて 'misoca_invoice'（揺れない）
  見出しの語がバラける        ★ ほぼ同じ（日付/品番・品名/数量/単価/金額）
                                揺れるのは『単価（税別）』『単 価（税抜）』の表記ゆれ止まり
  見出し行が 1〜3 行目        ★ 実物は 15〜17 行目（上に帳票のヘッダが 14 行ある）
  1 冊 = 明細行だけの表       ★ 実物は**帳票**。宛先はセル・明細は別ブロック・集計は下

実物が難しい軸（合成に 1 つも入っていなかった）
  帳票構造                    宛先は列でなくセル（B4 や B7 に『○○　御中』）
  金額の多義                  ★ 15/15 の冊で『金額らしい語』が 2 箇所以上
                                ご請求金額(税込) / 小計(税抜) / 合計金額 が全部ある
  結合セル                    43〜120 箇所（合成は 3 箇所）
  max_column                  内部で 256〜259 列（見た目は 14 列）
  複数シート                  ★ マネーフォワードは 1 枚目が『説明』・請求書は 2 枚目
```

### 実物で効いたのは 2 つのラベルだけ（40 行のコード）

```
宛先    『御中』を含むセル → そのセルから『御中』を除く      16/16
請求額  『ご請求金額』で始まるセル → 同じ行の右隣の数        16/16
```

★ **語の輪郭も・冊をまたぐ散らばりも・語のマップも・少数派の足切りも、1 度も使っていない。**
この設計（§2〜§3）が解こうとしていた問題は、実物では起きていなかった。

### 実物で崩れたのは抽出でなく**シート選び**

```
マネーフォワードの雛形   1 枚目『説明』（利用案内）／ 2 枚目『適格請求書（インボイス）』
既存の規約               1 枚目固定 → 説明シートを読む → ラベルが 1 つも無い＝取れない
★ 手がかりは既に在る     detect_header_row が説明シートで **None（確信なし）** を返した
                         「見出し行が見つからないシートは表ではない」をシート選びに使える
```

### だから設計はこう変わる（★ 未確定・書き直しは次）

```
格上げ   ラベル駆動の抽出（『御中』のセル・『ご請求金額』の右隣）
         シート選び（見出し行が取れないシートを飛ばす）
         金額の多義を**道具が決めない**（税込/税抜/明細の和のどれかを、依頼か人が決める）
降格     §2〜§3 の輪郭 → 語のマップ 一式
         ★ 消さずに保留にする。実物 2 ベンダは**どちらも雛形**で、
           人が自作した請求書では語も構造も揺れる可能性が残っている（未測定）
```

### この節の限界

```
16 冊のうち 15 冊が同一ベンダの色違い ── 「16 冊で 16/16」は「16 種類」ではない
中身が伏せ字（〇〇 〇〇）── 「取れた」は言えるが「正しい」は人が見るしかない
手作りの請求書は 1 冊も見ていない（Namakoo も持っていない・2026-09-10 時点）
```

---

## 0c. ★★ 実物 28 冊まで広げたら、0b の読みもまた外れた（同日 夜）

別ベンダの雛形（spreadoffice 5 冊）と官公庁の実表（総務省統計局 6 冊）を足して 28 冊にした。
**0b で「実物ではこうだ」と書いたことが、次の 5 冊で崩れた。** 記録として残す。

```
0b で書いたこと                     28 冊で分かったこと
  シート名は揺れない（misoca 統一）  ★ 納品書 / 請求依頼書 / 請求書 / 御請求書 / 説明
  ラベルは『ご請求金額』             ★ spreadoffice は 5 冊中 4 冊が『御請求金額』か『合計金額』のみ
  見出し行は実物で 15/15 当たる      ★ spreadoffice 3/5・官公庁 0/6 で None（確信なし）
  『御中』のセルが宛先               ★ 誤り。misoca 15 冊で担当者名を取っていた（下記）
```

### ★★ 自分の採点が嘘だった（この日いちばん重い失敗）

0b で「宛先 15/15 取れた」と書いたが、**正しくは 1/16 だった**。

```
misoca   B3 = ○○株式会社     ← これが取引先
         B4 = ○○ ○○　御中   ← 『御中』が付いているのは担当者名。ここを取っていた
inv21    B11 = abcd株式会社   ← 取引先
         B13 = 経理部　御中   ← 部署名。ここを取っていた
```

★ 気づけなかったのは**伏せ字**だから（`○○ ○○` も `○○株式会社` も もっともらしい）。
★ 真因は採点の作法を落としたこと ── 合成検体では `_答え.json` を必ず持っていたのに、
  実物に移った瞬間に**答えを持たずに「取れた」を数えた**。取れた ≠ 正しい。

### 『御中』ブロックの形もベンダで違う（1 つの形から一般化できない）

```
misoca      ○○株式会社 / ○○ ○○　御中                        2 段
inv21       abcd株式会社 / (空) / 経理部　御中                  2 段＋空行
spread_3    〒141-0031 / 〇〇… / 〇〇… / 〇〇…　御中           ★ 4 段（郵便番号から）
spread_1/4  〇〇〇〇〇〇 御中                                    ★ 1 段のみ
```

★ 28 冊を通して**共通しているのは『御中』の 2 文字だけ**で、その前後の形は揃わない。

### 官公庁の実表は、見出し行が 0/6

```
人口推計 第1〜3表 / 家計調査 表1〜3   すべて detect_header_row が None（確信なし）
  多段見出し・注釈行・結合セル（最大 202 箇所）
```

★ 請求書とは別の欠陥として立つ（「表を読む」層の実物での限界）。

### この日に 3 度繰り返した形

```
① 合成 40 冊    自分が難しいと思った軸（語ゆれ・シート名）をバラした → 実物では揺れない軸だった
② 実物 16 冊    『御中のセルが宛先』と読んだ → 次の 1 冊（inv21）で部署名だった
③ 実物 21 冊    『ブロックの先頭が取引先』と読んだ → 次の 5 冊で郵便番号が先頭だった
```

**毎回、手元にある形から一般化して、次のひと山で崩れている。**
設計を書き直す前に検体を増やしたのはこのため。

---

## 0d. ★★ 実装前に決着させたこと（同日 夜・Namakoo「あとから議題が増えると手戻り」）

答え付きの検体 B（`bench/received_invoices/`・骨は実物 3 種・受け取った請求書 40 冊）と
実物 22 冊で、設計を書く前に測った。**要件は Namakoo の 4 つの基準で言い直した**:

```
① 項目と関連する値をうまく抜き出せるか
② 請求書でもレイアウトが違うものが混ざっていても抜き出せるか
③ 抜き出しが正確で間違えていないか
④ 抜き出しに怪しい点があれば検分シートで指摘できるか
★ セル位置の指定は前提にしない（複数ファイルでは考えられない）
```

### 決着した測定（数字はすべて答え付きの検体 B・40 冊、または実物 22 冊）

```
現在地            ailine scan 14/40 ── 基準ファイルと同じ骨の 14 冊だけ。他 2 骨は 0
請求額            三口検算（上部ラベルの右 / 明細の和＋消費税 / 帯の合計）  40/40 全冊「確」・割 0
                  ★ 軽減税率が混ざっても誤報しない（消費税セルが区分ごとの丸め済み合計になっている）
請求元            R1 冊をまたいで散る       27/40（日付を除いても品目を掴む＝除外則の競争になる）
                  R2 版ごとに番地を固定     40/40 だが★実物 22 冊は 14 版に割れ 1 版 1 冊が 10 → 使えない
                  R3 連絡先(〒/TEL/登録番号)の直上・末尾「：」はラベルとして除外
                                            40/40（検体 B）／ 実物 18/22 が発行者名に着地・御中側 0
                                            残 4 冊は連絡先ラベル自体が無く「取れず」（正直な断り）
                  ★ R3 は版もセル位置も使わない。帳票の作法（発行者は連絡先の上に書く）だけ
走査窓            20 → 40 で帳票 27/40 → 40/40、官公庁は 0/6 → 1/6 の誤検出。器官の中だけ 40
明細ブロック終端  見ないと 27/40 で小計と食い違う（末尾まで足すと集計行を巻き込む）
検分（基準④）    壊した冊 4 通り（合計を 1 円ずらす／明細を 1 行消す／連絡先を消す／御中を 2 つ）
                  → 対照は「確」、壊した冊はすべて印が立つ（割・単・無）。印が立たない 0 件
                  ★ 合計を 1 円ずらした冊: 口①=1,101 / 口②=1,100 / 口③=1,101 と両側の数字が出る
```

### ★ 崩れた前提（この日の後半だけで 3 つ）

```
「版で番地を固定」   検体 B で 40/40 だったのは骨が 3 種しか無かったから。実物は 22 冊で 14 版
「散らばりで請求元」 日付を除くと品目に移るだけ。名簿の競争に入る
検体の答え          inv21 の請求元を B11（宛先ブロック）に置いていた → 正しくは H11（発行者ブロック）
                    ★ 基準を言い直してもらったから見つかった（今日 5 回目の検体の誤り）
```

### 要件・設計に足すもの（測定から出た）

```
要件  失敗の定義 ＝ 誤取得（確または単の印で違う値）。取れずは失敗ではない。誤取得 0 を必達に
      金額の多義は道具が決めない ── 税込・税抜の両方を列に出す（買い手条件④をここで満たす）
      式にキャッシュが無い冊は名指しで断る（表側の formula_columns_without_cache と同じ穴）
      請求元の検算: 宛先と同じなら誤り／連絡先ラベルと同じ塊なら誤り／末尾「：」はラベル
設計  項目はコードに焼かず宣言で持つ（名前・ラベル語の候補・型・検算の口）
      記録は 1 セル 1 レコード {項目, 値, 番地, 区分, 根拠, 対抗候補}。1 冊 1 行はその投影
      LLM 無しの経路（--fields）を先に立て、翻訳は「項目名のリストを返す」だけ
      表（縦積み）と帳票（項目集め）は器官を分け、共通の通貨は記録だけ
      検分シートに「怪しい」を出す口は既存の inspection.py
保留  1 冊しか無い連絡先なし帳票（spreadoffice 型）── 人に聞く口の設計は次
```

---

## 0e. ★★ 設計を 3 体に独立に書かせたら、俺の測定が 6 つ覆った（同日 夜）

§0d を「決着済み・再議論しない」と渡して 3 体の architect に設計本体を書かせた。
★ 3 体とも、その約束の上で**測って覆してきた**。全部こちら側の誤りだった。

```
① 検体の交絡         骨と明細行数が完全同期（misoca=1行 / 建設=2行 / inv21=3行）
                     原因は SKELETONS[i%3] と n_lines=1+(i%3) が同じ周期
                     ★「明細終端を見ないと 27/40」の 27 は骨 2 種の冊数(14+13)と一致 ── 分けられない
② ★★ 採点器が恒真   score_designB.py は**結合セルの写しを 2 口目に数えていた**
                     畳まない: 口が 3〜5 個 → 全冊「確」
                     アンカーで畳む: 口 2 個=14 冊 / 口 1 個=26 冊 → 正しくは 確 14 / 単 26
                     ★ 26 冊は**同じセルを 2 回数えて「2 口一致」**と言っていた
                     ── 買い手の $0 条件『検算が本体と同じ口から出る』を、自分の採点器がやっていた
③ 軽減税率           「誤報しない」は inv21 骨だけ。construction 骨は帯の作りが違い（G26=10%分/
                     G27=8%分/G28=合計）、最初に見つかる『消費税』が 10% 分なので割が出る＝誤報
                     ★ 俺の測定は inv21 の 1 冊だけだった
④ R3 は規則として未完成  候補が 1 冊あたり 2〜9 件出る。40/40 を出していたのは
                     **書かれていないタイブレーク（最上段）**。規則＝候補の作り方＋順位づけの 2 段で凍結する
⑤ R3 は誤取得を出す  宛先ブロックにも 〒/TEL がある冊（受領側の住所が刷ってある形）で自社名を返す
                     ★ 実物 22 冊で『御中側 0』だったのは**採点器だけが排除条件を知っていて
                       規則が知らなかった**から ── また片配線
⑥ 生成器の片配線     bench/received_invoices/mk_received.py が inv21 の issuer を B11(宛先側)のまま
                     README と作業場は H11 に直っていたが、追跡されている機械が古かった（★ 直した）
```

### ★ 3 体が独立に一致した設計の芯（ここは信じてよい）

```
帳票 ≠ 表。冊ごとに独立に読む（基準ファイル方式を使わない）
区分（確/単/割/無）は**導出関数 1 本**から。呼び出し側に文字列を書かせない
出口（画面・データ面・検分・json）は**同じ投影関数 1 本**から。片配線を作らない
op を DSL に足さず、scan/stack と同じ**部分コマンド**にする（12 表の束と語彙の共食いを避ける）
LLM は項目名の一覧だけ返す。セルの値は 1 バイトも渡さない。--fields を先に立てる
走査窓は定数を触らず**引数で渡す**（1 定数を 5 経路が読んでいる）
金額の多義は道具が決めない ── 税抜・消費税・税込を**別の列**として並べて出す
検分は既存 inspection.py に載せる（Finding は住所 3 座標が型で必須）
```

### 次にやること（順序も 3 体で一致した）

```
STEP 0  検体の交絡を解く（骨 × 明細行数 を直交させる・0行/8行を足す）
        採点器の口をアンカーで畳む（②の恒真を潰す）
        敵対検体を先に全部書き切る（宛先側に連絡先・連絡先なし・御中 2 つ・軽減税率×3骨）
        ★ 検体を書く人と規則を書く人を分ける（開発手法 §6d）
STEP 1  直した検体で測り直す ── 請求額の区分・請求元の規則（候補＋順位）・明細終端
STEP 2  その数字の上で設計本体（§1〜§9）を書き直す
```

★ **設計を書くのはまだ早い**、というのが今日の結論。§0d を「決着済み」と呼んだのは早かった。

---

## 0g. ★★ ゴール（2026-09-11・Namakoo 決裁 G2'）

設計を書く前に「どこまで届けば完成か」を決めた。★ **測った事実だけで書く**。

### 需要 5 件の現在地（全部この日に実測）

```
① 請求書 40 ファイルからの抜き出し   ★ 無い ── 帳票を読む器官が要る（検体 87 冊は用意済み）
② 入金明細と請求の突き合わせ         ★★ **既に届いている**
③ 経費の勘定科目                     無い ── 分類の知識が要る（別種の問題）
④ 部署別 30 ファイルの縦積み         ★ 10/30 ── **飾り行 1 つで落ちる**
⑤ 担当者別に分割して配布             1 冊内の帳票作成は在る／ファイル分割は無い
```

**②の実測**（請求 5 社 / 入金 4 社・答えは検体側が持つ）:

```
答え                     ailine run <請求> <入金> の出力
うえだ物産 3,000 不足     ⚠ うえだ物産: +3000（A 43000 / B 40000）      ○
大久保商店 未入金         ⚠ 大久保商店: A のみ（A 66000 / B なし）      ○
3 社は一致                差額なし 3 件                                 ○
```

★ 「A のみ」と「差額あり」を区別して名指しし、両側の数字を並べている ──
買い手の信用条件（分母・名指し・両側の数字）を満たす形で**既に動いている**。

**④の実測**（部署別 30 冊・飾り行 0〜2 行・3 冊に 1 冊は合計行つき）:

```
答え      明細 120 行 / 合計 762,000
出力      40 行 / 236,000 ── 積めたのは**飾り行が 0 行の 10 冊だけ**
落ちた形  「余り: 〇〇課 経費明細」＝ 飾り行を見出しと誤読
★ ただし 合計行の除外は 10/10 で正しい（二重計上の罠は突破している）
```

### ★ ゴール: G2'

```
①を作り、その器官で④も救う。②は既に届いているので触らない。

  届く範囲   需要 ①②④ ＝ 上位 5 件のうち 3 件
  作るもの   帳票を読む器官（①）
             ＋ 見出し行を**冊ごとに**推定する（④の直し・①と同じ器官）
  触らない   ② 届いている ／ ③ 別種（分類の知識） ／ ⑤ 配布は別の話
```

★ **1 つ作れば 2 つ届く** ── ④が落ちる原因（基準 1 冊の見出し行を全冊に当てる）は、
①で作る「冊ごとに読む」器官がそのまま解く。①のついでに④が直るので、④に専用の工事は要らない。

### 完成の言い方（★ 測った範囲でだけ語る）

```
「請求書の山から項目を集めます。部署別ファイルの縦積みも直ります。
  入金と請求の突き合わせは既にできます」
```

★ ③⑤を入れない理由: **測っていない領域が混ざる**。③は分類の知識（辞書か学習）が要り、
⑤の配布はファイルを分けて書き出す話で、どちらもこの器官の延長線上に無い。
発火条件つきで保留する ── 実需が来たら測ってから決める。

### 完成の判定（★ 実装前に凍結する）

```
必達  誤取得 0        「確」または「単」で出した値が答えと違う件数（検体 87 冊）
必達  空欄に理由      空欄の数 == 理由の数、かつ理由が成果物に照らして本当（§0f）
必達  混入で沈黙      官公庁の実表 2 冊で値を作らない
必達  ④が救われる     部署別 30 冊で 30/30 積める（いま 10/30）
開示  到達率          ★ バーを置かない（置くと『割』を『単』に格下げする方へ手が動く）
```

---

## 0f. 凍結した判断（2026-09-11・採点の前に決める）

検体を書いた側からの申し送り ──「**割 と 単＋指摘 の線引きは検体側の判断。
買い手がどちらを許すかは聞いていない。採点の前に凍結してくれ**」。Namakoo が決めた。

```
★ 食い違ったら **値を出さない（空欄）**。両側の数字は検分シートに出す

  割（値を出さない）   根拠が 2 つ以上あって食い違う
  単（値を出す＋印）   根拠が 1 つだけ（食い違ってはいない・裏が取れていないだけ）
  無（値を出さない）   根拠が無い
```

根拠:

```
① 空欄は誤値より安い ── 経理の表で、間違った金額が入っているより空いている方が直せる
② $0 条件『黙って失敗する』から最も遠い ── 空欄 + 検分の名指しは、黙っていない
③ 単 と 割 を分ける意味がここで立つ ── 単は「まだ裏が無い」、割は「矛盾している」。
   矛盾を値で出すと、人はどちらが正しいか分からないまま片方を受け取る
```

★ 凍結する ── この線引きを**採点の後で動かさない**。動かすと、数字が良くなる方へ
手が動く（割を単に格下げすれば到達率が上がる）。変えるなら理由と日付を残して測り直す。

★ `--fill-guess` のような「印つきで値も書く」口は**作らない**（この版では）。
  発火条件: 実物のフォルダで割が何件出るかを測り、人が空欄を埋め直す手間が
  「30 分で正解」を超えたとき。

### ★★ 空欄には必ず理由を添える（Namakoo 追加・同日）

**空欄だけだと「取れなかった」と「矛盾していた」が同じ顔になる。** 買い手には区別がつかず、
「この道具は空欄を返してくる」という 1 つの印象に潰れる。区分を分けた意味が画面で消える。

```
★ 空欄のセルには、必ず**その場で**理由が読めること
   出力ブック   空欄セルにコメント（セルにぶら下がる）── 表の見た目は汚さない
                隣の区分列に 割 / 無 を出す
   検分シート   1 行 1 件で「なぜ空欄か」を名指し
                割 → 両側の数字と、それぞれの番地
                無 → 何を探して見つからなかったか（ラベル名・探した範囲）
   画面         件数と、最初の数件を名指し
```

★ 実装の縛り: **空欄を書く経路は、理由を持たずに呼べない形にする**（引数で強制する）。
  「理由を後から足す」形にすると、片方の経路だけ理由なしで空欄を書く事故が必ず生える
  ── この repo が何度も踏んだ片配線（docs/開発手法.md §13）。

★ 番人①（在ること）: 空欄のセルすべてに対応する所見が検分シートに在ること。
  空欄の数 == 理由の数 でなければ赤。片方だけ増える変異で赤くなることを確かめる。

### ★★ 番人②（適切であること）── Namakoo 追加・同日

**「空欄の数 == 理由の数」は、理由が定数文字列でも満たされる。**
「見つかりませんでした」を全件に書けば通る ── この repo が繰り返し潰してきた恒真の形。
**理由が適切か**を機械で確かめる線を、実装の前に引く。

```
(a) 区分と整合している
      割 → 両側の値と、それぞれの番地が在る（片側だけなら赤）
      無 → 何を探したか（ラベル名・探した範囲）が在る
      ★ 区分から理由の形が決まるので、導出関数と同じ 1 本から出す

(b) ★ 成果物に照らして本当である（これが「適切」の核）
      理由が番地を名指ししたら、**その番地が実在する**こと
      理由が値を挙げたら、**そのセルの実際の値と一致する**こと
      理由がラベル名を挙げたら、**その語が冊のどこかに在る**こと（無 の理由なら在らないこと）
      ★ 検算は別実装で読み戻す（本体の走査結果を再利用しない ── 再利用すると恒真）

(c) 具体が入っている（定数でない）
      理由に番地・値・ラベル名のいずれも含まない所見が在ったら赤
      ★ 87 冊の理由の**異なり数**を出す。1 種類しか無ければ、それは定数

(d) 検体が期待する名指しを含んでいる
      検体側の答えに『名指し』が在る 26 冊は、その語が理由に出ていること
      ★ 残り 61 冊は検体側から採点できない ── (a)(b)(c) で見る
```

★ **機械で確かめられないもの**（正直に書く）:

```
その理由を読んだ人が、次に何をすればよいか分かるか
  → 機械は「番地が実在する」までしか言えない。**人が読むしかない**
  → 実装したら、Namakoo に実物の検分シートを 1 枚見せて判断を仰ぐ
  ★ 既存の前例: 検分シートの薄い色は「気づかない」で一度却下されている
    （src/ailine_core/inspection.py の経緯）── 画面は人が見て決める領分
```

---

## 1. 何を作るか（範囲）

```
入力    フォルダ（直下の .xlsx・サブフォルダは見ない・既存の scan と同じ規約）
依頼    「取引先と請求額を抜き出して」のように**集める項目**を挙げる
出力    新しいブック 1 冊（原本は触らない）
          1 明細 = 1 行・先頭に出所列（ファイル名）・★ 合計行は除く
          ＋ 報告（§4）
```

**やらないこと（この版）**: 集計（和・件数）は出さない。既存の集計 op に渡す。
明細が複数行ある冊は複数行のまま出す。★ 「1 冊 1 行にまとめて」は別の依頼。

---

## 2. 2 パス

```
パス①  インポート（読むだけ・LO を起動しない）
   1  各冊: 見出し行を推定（★ 冊ごとに。基準 1 冊の行番号を全冊に当てない）
   2  各冊: 各列の輪郭を取る  空率／日付らしさ／数らしさ／文字値の集合
   3  冊をまたいで束ねる      見出しの語ごとに輪郭を集める
   4  役割を決める（§3.1）    語は 1 つも読まない
   5  少数派を捨てる（§3.2）  分母が要る判断
   6  語のマップ              役割 ← {語: 冊数}

パス②  抽出
   7  依頼の項目（取引先／請求額…）をマップの役割に当てる
   8  各冊: 見出しの語でマップを引く → 引けなければ輪郭で拾い**「推測」の印**を付ける
   9  合計行を除いて行を集め、新ブックに書く
  10  報告（§4）
```

★ 既存の `ailine scan` はパス①の 1〜2 を「基準 1 冊だけ」やっている。**基準 1 冊の方式を
「多数派の輪郭」に置き換える**のがこの設計の中心。

---

## 3. 判断の中身

### 3.1 役割は輪郭で決める（語を読まない）

```
備考      空率の平均 > 0.8                 全冊ほぼ空
日付      日付らしさの平均 > 0.5
金額      数らしさの平均 > 0.5             ★ 「1,000円」「¥12,000」は数として読む
取引先    文字が並び、**冊をまたいで値が散る**（union の大きさ / 冊数 > 0.5）
固定ラベル 文字が並ぶが全冊ほぼ同じ値
```

★ 「冊をまたいで散る」が要点。1 冊の中の一意率では、明細 1 行の冊で取引先と備考が
区別できない（当日の実測: 推測 17 件はほぼこれ）。**横に見て初めて分かれる。**

★ 閾値は当日の検体で置いた仮の値。**凍結して未見の検体で測る**（docs/開発手法.md §6d）。

### 3.2 少数派を捨てる（分母が要る）

```
MIN_BOOKS = max(2, N // 20)
N 冊中 MIN_BOOKS 冊未満にしか出ない見出しの語は、マップに入れない
```

理由: 見出し行を読み違えた冊は、**データの値を見出しとして持ち込む**
（当日: `大久保商店(1冊)` `5,250円(1冊)` がマップに混ざり 39→37 に落ちた）。
「40 冊中 1 冊だけの語は語でなくデータ」は、**N が在って初めて言える**。
★ 捨てた語は報告に出す（黙って捨てない）。

### 3.3 マップで引けなければ輪郭に落ち、「推測」と印す

見出しが空の列（列順が違う冊で起きた）はマップに乗らない。輪郭で拾う。
★ ただし**その冊は「推測」区分**にする。買い手の信用条件③
「取れた／取れなかった／推測で埋めた の 3 区分」を、ここで機械の値として持つ。

### 3.4 見出し行は各冊で推定し、横の証拠で疑う

```
各冊: detect_header_row（既存）で推定
横:   列数の最頻値と違う冊、役割が 1 つも当たらない冊 → 「見出し行を疑う」と報告
```

★ 当日の残 1 冊（請求書_05）は、飾り 1 行＋見出し＋明細 2 行で、推定が明細側に寄った。
**直すのは推定器でなく報告**（推定器を検体 1 つに合わせて動かさない）。
横の証拠で「この冊だけ 4 列でない」と名指しできれば、人が直せる。

### 3.5 マップは成果物

```
画面    「取引先 ← 御中(10冊)・宛先(10冊)・顧客名(9冊)・取引先(8冊)」の形で出す
直す    人が 1 行直せる（既存の attr/alias と同じ口）
覚える  次回、同じフォルダ／同じ語なら聞かずに使う（attr に載せる）
```

★ **LLM に列の役割を聞かない**。需要地図の $0 条件「同じ入力で結果が変わる」に触れる。
将来やるとしても候補の提示までで、確定は輪郭か人。

---

## 4. 報告（買い手の信用条件をそのまま）

```
分母             40 冊中 39 冊から取れました
名指し＋理由     請求書_05: 見出し行を 4 行目と読みました（他の 39 冊は 4 列・この冊は 1 列）
推測の区分       取れた 37 ／ 推測で埋めた 2（見出しが空の列を輪郭で拾った）／ 取れなかった 1
両側の数字       出所の明細 61 行 → 出力 61 行（合計行 10 件を除外）
捨てた語         「大久保商店」「5,250円」（1 冊にしか出ないのでデータと判断）
失うもの         罫線・塗り・数式は持ち越しません（既存の stack と同じ文言）
```

★ 当日の実測で、`scan` と `stack` は**既にこの形で報告している**（分母・名指し・
シート名の推測を明示・両側の数字）。壊れていたのは到達であって正直さではない。
ここは既存の形をそのまま使う。

---

## 5. 既存との接続（★ 片配線を作らない）

| 既存 | いま | この設計 |
|---|---|---|
| `multifile.evaluate_file(…, header_row, …)` | 基準の行番号を全冊に当てる | 冊ごとに推定した行を渡す（検出器は呼び出し側から**関数で**渡す ── multifile は ailine を import しない規約） |
| `cmd_scan` の基準ファイル方式 | 1 冊目が基準 | 多数派の輪郭が基準。1 冊目は特別扱いしない |
| `cmd_stack` | 同上 | 同じ 1 関数から header_row を受ける（**scan / stack / run の 3 経路を 1 本に畳む**） |
| `run <folder>`（行の絞り込み） | 別の操作 | 触らない。新しい op として「項目を集める」を足す |
| `attr` / `alias` | 語 → 実表の何か | マップの「覚える」の置き場 |

★ 2026-08-24 の教訓（同じ形の経路すべてを 1 関数に畳む・番人は 1 本の試験で全経路を縛る）
をここで使う。header_row を「冊ごと」に変えるなら、**3 経路が同じ関数から受ける**形にする。

---

## 6. 検体と採点

```
bench/folder_map/mk_invoices.py     40 冊を合成（崩れ 3 点を意図的に入れる）
bench/folder_map/_答え.json         ★ 検体側が持つ答え（道具の出力から作らない）
bench/folder_map/build_map.py       設計どおりの 2 パスを素の Python で通す（LO 不要・秒で終わる）
```

★ **再現の粒度**（当日 実測）: 2 回生成して突き合わせた ──
**中身（シート名・全セルの値）は完全に一致**（sha256 の先頭 `cc9d39fa0f9ab0fd`）するが、
**バイトは一致しない**（xlsx の `docProps/core.xml` に作成時刻が埋まる）。採点は 39/40 で不変。
★ だからこの 40 冊を `tests/golden/` の作法（バイト一致）で凍結すると**毎回落ちる**。
凍結するなら中身のダイジェストで縛る。

★ **限界を先に書く**: 検体は俺が作った。日付は `2026-08-xx` 固定、金額は整数か `1,000円`。
実物の請求書は和暦・`¥12,000-`・注釈つき・空セルがある。**39/40 は上限の見積もり**であって、
実物で同じ数字が出る保証ではない。実物で測る前に、この数字を看板に使わない。

---

## 7. 番人（実装と同時に置く）

```
事前語ゼロ      役割を決める関数に、語の定数が 1 つも無いこと（inspect で縛る）
分母依存        少数派の足切りが N に依存すること（N=2 と N=40 で閾値が変わる）
推測の区分      マップで引けなかった冊が「推測」として報告に出ること
片配線          header_row の「冊ごと」推定が scan / stack / 新 op の 3 経路で
                **同じ関数**から来ること（変異で 3 経路が同時に赤くなる）
検体の答え      採点が _答え.json から来ること（道具の出力を期待値にしていない）
凍結            閾値（0.8 / 0.5 / N//20）を変えたら未見の検体で測り直す
```

---

## 8. 決めないこと（発火条件つき）

```
和暦・全角数字・「¥12,000-」   当日の検体に無い → 実物の検体が来たら測る
明細複数行を 1 行にまとめる     別の依頼として扱う → 需要が来たら
LLM による候補提示              輪郭で決められない冊が実物で何割出るかを見てから
複数シートの冊                  1 枚目固定（既存の規約）→ 実物で 2 枚目が要る例が来たら
```

---

## 9. 実装の順序

```
1  header_row を冊ごとに（3 経路を 1 関数へ）＋ 番人      ★ 挙動が変わる commit
2  パス①（インポート＋輪郭＋役割＋足切り＋マップ）を ailine_core に
3  パス②（マップで抽出＋推測の印＋報告）＋ 新 op の語彙
4  検体と採点を bench/ に
5  attr への「覚える」
```

1 は単独で出せる（scan の到達 1 → 2 だが、**基準 1 冊方式の穴を先に塞ぐ**）。
2〜3 が本体。4 は 2 と同時。5 は後でよい。

---

## §1 帳票を読む器官 ── 作って測った（2026-09-11）

### 1.1 作ったもの

```
form_grid.py     座標と中身だけ。意味を持たない。結合はアンカーに畳む
form_read.py     「どこを見れば請求元か」の規則
field_record.py  集めた根拠から区分（確/単/割/無）を導く ── 区分を作るのはここだけ
```

`tests/test_a_grade_has_exactly_one_source.py` が「区分の語を持てるのは
`field_record.py` だけ」を AST で縛っている（実装より先に置いた番人）。

### 1.2 測り方（★ 点数を見る**前**に凍結した）

- **分母** = 検体を書いた側が宣言した `期待` の件数（171）。★「取れた数」を分母にしない
- **正** = 区分が許された組に入る ＋ 値が一致 ＋ 禁止値でない ＋
  空欄なら理由が `名指し` の語をすべて含む
- **見出しにする数は正答率ではない**:

  | | 意味 |
  |---|---|
  | 誤報 | 値を出して間違えた ← **最悪**（$0 条件『黙って失敗する』） |
  | 安全 | 空欄にして理由も名指せた ← 取れていないが害は無い |
  | 不親切 | 空欄だが理由が足りない ← 空欄の意味が人に伝わらない |

- 採点器は `field_record` を **import しない**（同じ関数で作った分母は分母でなく感想）
- 対照 2（答えをそのまま返す／何も返さない）＋ 変異 3 を、
  「> 0 なら合格」ではなく**答えから算術で出した正確な件数**で通す
- 群を絞ると当てる対象が無い変異が出る。そこは**「測れなかった」**と分けて言う

★ 採点器そのものに欠陥が 2 つあった（どちらも変異で露見）:
理由が空の空欄を 134 件「安全」と呼んでいた／正解値を指定しない冊で
**正しい抽出を誤報と呼ぶ**ところだった。

### 1.3 数字

規則は **基礎 36 冊だけ**を見て書き、凍結してから残り 51 冊を開いた。

| | 開発群 基礎 36 冊 | 未見 51 冊 |
|---|---|---|
| 凍結直後 | 108/108 | **43/63 (68.3%)** ／ 誤報 6 |
| 未見を見て直した後 | 108/108 | 63/63 |

★ **意味があるのは 68.3% の方**。100% は「敵対群に当てはめきった」ことしか
言っていない。51 冊はもう未見ではない。

**一度も測っていない実物 26 冊**（政府統計 6・vendors 5・空の雛形 15）── 答えは無いが:

- 政府統計の表 6 冊: 宛先・請求元・請求額とも全部 `無`。請求書でないものに一言も答えない
- 空の雛形 15 冊: 宛先・請求元とも `無`（プレースホルダを名前と認めない）
- 落ちた冊: 0

### 1.4 実物が教えたこと（想像で書いた規則は 1 つも残っていない）

| 実物の形 | 素直な規則が壊れる所 |
|---|---|
| `B3=社名` / `B4=経理部　御中` | 御中が付くのは**部署の側**。会社名は上 |
| `B7=社名　御中` | 同じセルに同居する骨もある。**両方要る** |
| misoca: 社名→〒 ／ inv21: 〒→住所→社名 | 「〒の上」は骨に依存し一般形にならない |
| 宛先ブロックと発行者ブロックが同じ形 | 難所は「探す」でなく「**どちらがどちらか**」 |
| `E38=消費税 | G38=0.1 | H38=6900` | いちばん近い右は**税率** |
| `O19=合 計 金 額` / `B22=ご請求金額⏎（消費税込）` | 照合前に空白を全部落とす |
| `C28=小計 E28=…` / `G28=…` | 帯には**積み上げ型**と**表型**があり列が揃わない |
| `¥1,320,000-` / `１，３２０，０００` / `#REF!` | 枠は埋まっている。**跨いで別の数を読まない** |
| `社名⏎(登録番号:T…)` | 1 セルに改行で同居する |
| `株式会社 〇〇〇` | 雛形のまま＝名前ではない |
| `（取次）…　担当 井上` | 取次・担当の行は発行元の名前ではない |
| `B42=お振込金額　121,790 円` | **ラベルと金額が同じセル**に入る |

### 1.5 ★ 未見が教えた 2 つの構造（器官そのものより重い）

**① 「見ていないから自信がある」**

凍結直後の誤区分 12 件すべてで、俺が開いた 2 つの口は**一致していた**。
食い違っていたのは開いていない第三の口だった ── 源泉徴収の振込金額・
備考の再掲金額・明細の合計・2 枚目のシート・通貨・そもそも納品書か見積書か。

★ **一致は「他の口が黙っている」ことを意味しない。開いていないだけだ。**

→ `確` の意味を変えた:

```
確 = 独立した口が 2 つ以上一致した ＋ 食い違う口が無いことを**掃き出して確かめた**
     掃き出していないなら、言えるのは 単 まで
```

これは「到達できず＝未確認であって安全でない」と同じ線。

**② 食い違いが区分に届いていなかった（片配線）**

明細と小計が合わない冊で「**単・値あり・理由は食い違い**」という記録が出ていた。
理由の文字列にだけ書いて、区分の導出は根拠しか見ていなかった。
→ 食い違いは散文ではなく**導出への入力**にした。

**同じ形をもう 1 回踏んだ**: プレースホルダの番人を請求元の側にだけ書いていて、
宛先は空の雛形に「○○株式会社」を値として出していた（一度も測っていない実物で発覚）。
処方は「両方直す」ではなく **1 関数に畳んで呼び出し側に選ばせない**。
番人も **1 本の試験で両方の項目を縛る**（片方を壊せば必ず赤くなる）。

### 1.6 途中で自分の直しが壊したもの（開発群を毎回測って捕まえた）

- 税区分表の `対象額（税抜）` を小計のラベルとして入れ、10% 欄を小計と誤認 →
  8% だけの冊で「明細 110,000 と小計 0 が合わない」と**偽の食い違い**
- 明細の終端を「小計の行」で切り、税区分行の消費税額まで明細に足した →
  117,000 + 11,700 = 128,700 と**数が合ってしまう**（気づきにくい形）
- 名前の処理を 1 関数に畳むとき、請求元側の都合（敬称つきの行を捨てる）を
  持ち込んで宛先を 13 冊壊した

### 1.7 ★ 重ねた守りは番人を盲目にする

敬称を外す処理を 3 か所に重ねていた（行を捨てる前・除外語の表・最後）。
どれか 1 つを壊しても他が肩代わりするので、**変異試験が全部緑になった**。
→ 守りを 1 か所に畳んで、壊したら赤くなるようにした。

★ これは「番人が在っても、その事故の形では鳴らない」の新しい顔だ ──
今回は**自分で重ねた冗長が鳴らなくしていた**。

### 1.8 いま守られているもの

- 区分の導出への変異 **9/9**（陽性対照込み）
- 読む規則への変異 **9/10**（残り 1 つは等価な変異 ── 直しても振る舞いが変わらない）
- 検体 87 冊 171/171、対照 2・変異 3

### 1.9 残っている宿題

- **冊全体の判断**: 空の雛形で請求額だけ `確 3,300` が立つ（雛形の見本行）。
  「この冊は雛形だ」は項目ごとの器官の仕事ではなく、複数ファイルを束ねる層の仕事
- **請求元・宛先は口が 1 つしかない**ので原理的に `確` にならない（全部 `単`）。
  裏を取る手（登録番号との突き合わせなど）を足すかどうかは値付けと相談
- **2 枚目のシート**を読んでいない（1 冊に請求書 2 枚の冊は敵対群にあった）
- 一般化を主張するには**新しい未見の検体**が要る。いまの 51 冊はもう未見ではない

### 1.10 ★ 測定そのものに穴が空いていた（同日・追記）

§1.3 の数字は、実は**買い手が持っていない手がかり付き**だった。

実行器が「どのシートを読むか」を**検体の答えから受け取っていた**。
87 冊中 14 冊は複数シートで、その内訳は:

| 形 | 冊数 |
|---|---|
| 1 枚目が `説明` シートで、本体は 2 枚目 | 12 |
| 1 冊に請求書が 2 枚（8 月分・9 月分） | 1 |
| 本体のシートが**非表示**で、可視シートは空の 1 枚だけ | 1 |

答えを渡していれば、この 14 冊の問題は**一度も解かずに**満点になる。

★ 気づいた経緯: 「2 枚目のシートを読んでいない」を宿題として書いたあと、
  実際に何冊が複数シートなのかを数えに行って分かった。
  **宿題を数字にしようとした瞬間に、自分の測定の穴が見えた。**

処方:

- 器官が**自分でシートを選ぶ**層（`read_book`）── 請求書らしい印が
  **別の種類で 2 つ以上**そろったシートだけを候補にする
  （印 1 つだと送付状の「御中」を 2 枚目の請求書と数えて偽の食い違いが出る）
- 候補が 2 枚 → 全項目を空欄にし、理由で「シート」を名指す
- 非表示のシートを読んだら**そう言う**（黙って隠しシートを読まない）
- どこにも無ければ**見たシート名を並べる**（「探し方が悪かったのか」を人が判断できる）
- ★ 実行器から**答えを渡せる口ごと消した**（引数を残すと、次にまた渡す）

手がかり無しで測り直して 171/171・誤報 0。

★ もう 1 つ、`grade_of` の逃げを塞いだ ──
食い違いを見つけていても根拠が 0 個なら `無` を返していた。
請求書が 2 枚ある冊に「手がかりが見つかりません」と言うのは嘘で、
正しくは「**決められない**」。人は前者を見ると探し方を疑う。

### 1.11 ★ この日にあらわれた「番人が鳴らない」形の一覧

今日 1 日で 4 通り出た。どれも「番人が無い」ではなく「**在るのに、その事故の形では鳴らない**」。

| 形 | どう分かったか |
|---|---|
| 重ねた守りが互いを隠す（敬称を 3 か所で外していた） | 変異を当てたら**どれを壊しても緑**だった |
| 相対 import が依存の図から消える | モジュール数だけ 64→67、辺は 84 のまま |
| 測定が答えから手がかりを受け取っていた | 宿題を数えに行って気づいた |
| 食い違いを見つけても区分に届かない（片配線） | 「単・値あり・理由は食い違い」という記録が出た |

---

## §2 需要④ 部署別ファイルの縦積み ── 見出し行を冊ごとに探す（2026-09-11）

G2' は「①を作り、その器官で④も救う」だった。④ は**器官を当てるまでもなく**、
同じ「基準 1 冊に固定する」病で落ちていた。

### 2.1 実測

| | 朝（直す前） | 直した後 |
|---|---|---|
| 部署別 30 冊の縦積み | 10/30 冊・40 行・Σ236,000 | **30/30 冊・120 行・Σ762,000** |

120 行 / 762,000 は**検体を作った側が持っていた答えと完全一致**。

### 2.2 原因

`cmd_stack` は見出し行を**基準 1 冊から 1 回だけ推定**し、その行番号を全ファイルに
当てていた。実物のフォルダは、同じ様式でも飾り行の数が違う ──
30 冊の見出し行は **1 行目 10 冊 / 2 行目 10 冊 / 3 行目 10 冊**。

★ 検体を書いた者（規則を知らない者）が、あらかじめ「落とし方」に
  **『基準 1 冊の番地に固定した実装』**と名指ししていた罠でもある。

### 2.3 ★ 同じ欠陥が 3 つの実装に写し取られていた

```
stack.evaluate_and_stack           ailine stack
extract_multi.evaluate_and_extract ailine run <フォルダ>
multifile.evaluate_file            棚卸し
```

→ **3 つ直すのではなく `multifile.find_header_row` 1 本に畳んで、3 つが呼ぶ**。
  判定そのものも `classify_headers` を使い回す（ここで比較を書き直さない ──
  書き直した瞬間、片方だけ直る経路が生える）。

表示も 2 箇所に在った（`render_stack_report` と `cmd_run_folder` の自前 `say`）。
両方に配線し、**番人は 1 本の試験で 2 経路を縛る**（parametrize）。
片方を黙らせたら必ず赤くなることを変異で確かめた。

### 2.4 緩めなかった線

「行を探しに行く」を部分一致で許すと、**列が本当に違う冊**がどこかの行で
たまたま一致して積まれる（`test_unmatched_file_is_skipped_named_and_counted`
が守っている契約が骨抜きになる）。
→ 採用するのは **`classify_headers` が「取れた」と言う行だけ**。負の被覆で縛った。

### 2.5 ついでに出た欠陥

- **`--json` が積めなかった冊を名指ししていなかった**。
  `denominator` と `stacked_files` の**数だけ**で、どの冊かは人向けの表示にしか無い。
  自動化する側は「30 冊中 10 冊」とだけ知らされ、残り 20 冊を聞けない。
  ★ 同じ事実に出口が 2 つあって、片方にだけ名前が載っていた。
- **最初に書いた試験の 1 本が `skip` で緑に見えていた**（`run` 経路の呼び方を間違えていた）。
  ★ **skip は番人ではない** ── その経路が裸のまま緑に見える。呼び方を直して本物にした。

### 2.6 開示

見出しが基準と違う行に在ったら、**必ずそう言う**:

```
  経費_02_営業二課.xlsx: 見出しは 2 行目にありました（基準は 1 行目）
```

黙って別の行を読むのは、この repo でいちばん高くつく失敗の形
（今日 1 日で ①文字の金額を跨いで別の数を読む ②隠しシートを黙って読む
  ③基準と違う見出し行を黙って読む の 3 つが同じ顔で出た）。

---

## §3 請求元は位置で決めない（2026-09-11・B′）

### 3.1 先に測った ── `確` を出す側は実物に支えられていない

Namakoo の依頼は「登録番号や 〒 と突き合わせて請求元・宛先に `確` を出す」だった。
作る前に、**名前の 2 つ目の口が実物に在るか**を測った。

| 口 | 結果 |
|---|---|
| 同じ名前が別のアンカーに 2 回 | 請求元 81/83 冊が 1 箇所・宛先 82/85 冊が 1 箇所。本物の裏取りは **1 冊** |
| 登録番号で国税庁を引く | ★ **失格** ── データが外に出る（買い手の $0 条件） |
| 文書の属性（作成者・会社） | 0/83。入っていたのは **弥生株式会社（90 冊）**・雛形を書いた人・openpyxl |
| 振込先の口座名義 | 0/83（1 件当たったが 〇〇〇 同士の偽陽性） |
| ファイル名 | 中身から独立で実フォルダでは濃いが、**検体の名前が `recv_B01.xlsx`** で測れない |

★★ 3 番目が危なかった: 文書の属性を裏取りに使っていたら、**90 冊で「請求元＝弥生株式会社」を
`確` として出す偽の裏取り装置**になっていた。雛形を作った会社を、請求を出した会社として。

→ **名前の `確` は冊の中身からは立たない。** ここで「それらしい 2 つ目」を作れば、
この repo がずっと潰してきた恒真を、こちらから作ることになる。

★ 登録番号は使わないのではなく、**役割の証拠として使い、値の証拠にはしない**。
登録番号は「このブロックが発行者だ」とは言うが、「名前が○○だ」とは一言も言っていない。

### 3.2 生きている半分 ── 決められないなら決めない

`確` に上げる装置は無いが、同じ掃き出しの**逆向き**（`割` に落とす側）は生きている。
いまの `read_issuer` は候補が複数残ると「右の列を優先」していた ── 実物の版面の**癖**だ。

測った:

```
候補が 1 つだけ      78 冊   ← 癖は一度も走らない
癖で決めていた        2 冊   ← T10・T11
候補なし              3 冊
シートが決まらない    4 冊
```

**癖が運んでいたのは 2 冊**。そして 2 冊とも、競合は「別の行に再掲された**宛先**」だった:

```
T10  御請求先：ナギ商会株式会社／9 月分     ← 宛先の再掲（検体の禁止値）
T11  ナギ商会株式会社                       ← 宛先（写しの支店宛）
```

宛先の除外を **完全一致 → 含む** に変えると T10 の競合が構造的に消え、**79 冊が候補 1 つ**。
巻き添えで正解を消した冊は **0**。残る T11 は宛先そのものが `割` で買い手を除けないので、
決められないのが正直（検体も請求元の期待を宣言していない）。

→ **癖を消しても失うものが無いことを測ってから消した。** 実測 171/171 は変わらず。

### 3.3 設計は小さくなった

候補を 1 つに絞ってから記録を作るのをやめ、**生き残った候補を全部 `Evidence` に渡す**。
1 つなら `単`、値が違う 2 つなら `割` ── 区分の導出が既に知っている仕事なので、
ここで新しい判断を書かない。分岐が 1 本消えて正味で短くなった。

### 3.4 ★ 正直に残す穴

① の「含む」は、**社名が入れ子**のときに正解を巻き添えにしうる
（宛先「トヨタ自動車」／請求元「トヨタ自動車東日本」）。
いまの検体に入れ子の社名は 1 つも無いので、**測れていない**。
★ 発火条件: 入れ子の社名を持つ検体が入った時、または「請求元が空欄になる」報告が来た時。
★ 検体は**規則を知らない側**に頼む ── 俺が書くと自分の答えを自分で採点することになる。

### 3.5 B′ が良くしないもの

口は 1 つのままなので、**`確` は依然として立たない**。
B′ が良くするのは「間違った名前を出さない」側だけで、確信の度合いは上がらない。

### 3.6 ★ 入れ子の社名を測った（2026-09-11・§3.4 の穴を閉じた）

§3.4 に「測れていない」と書いた穴を、**規則を知らない側に検体を頼んで**測った
（14 冊・群「入」。実装（`form_read.py` ほか）を読むことを明示的に禁じて依頼した ──
読まれたら俺の実装でなく俺の説明に当たる検体になる）。

```
正 18/25 (72.0%)   ★誤報 0   安全 5   不親切 1   誤区分 1
宛先 11/11 ／ 請求元 7/14
```

**★ 誤取得は 0。** 壊れ方は全部「空欄にする」側に倒れた ── 凍結した判断（空欄は誤値より安い）
がそのまま効いている。

外れた 7 件を原因で割る（★ 一括りにしないこと。3 つの別の穴が混ざっていた）:

| 原因 | 冊 | 中身 |
|---|---|---|
| **法人格の無い名前を組織名と認めない** | 3 | `ナギ商会`（略称）・`ナギ商店`（屋号）・ブランド名 |
| 「含む」による巻き添え | 2 | `ナギ商会株式会社サービス` ／ 同＋`福岡支店` |
| 宛先が `割` で買い手を除けない | 1 | 備考の同接頭辞企業と競合して `割`（構造としては正直） |
| 誤区分 | 1 | 発行代行（BPO）── 道具は `単`、検体は「決められない」 |
| 不親切 | 1 | 理由が検体の名指し語を含まない |

★★ **俺が名指しした穴（入れ子の巻き添え）は実在した。だが、それより大きい穴を
俺は名指しすらしていなかった** ── 法人格の無い名前（3 冊 vs 2 冊）。
`_looks_like_org` は `株式会社` などの語尾を要求するので、略称・屋号・ブランド名は
**候補にすら上がらない**。個人事業主と屋号は実務では普通にある。

★ これが「規則を知らない側に検体を書かせる」ことの値打ち ──
自分で書くと、自分が知っている穴の周りしか検体にできない。

### 3.7 直す前に決めること（未着手）

`_looks_like_org` を緩めるのは**語彙を広げる**話で、この repo が繰り返し踏んだ
「op を足すと近い op から静かに奪う」と同じ家系にある。緩めた瞬間、
住所・担当者名・銀行名が候補に入りうる。

★ だから直すなら、**緩める前後で 87 冊を測り直す**のが先。
「入れ子 14 冊が良くなって 87 冊が悪くなった」を見逃さないため。
★ 発火条件: この 3 つの穴のどれかに実需が当たった時、または Namakoo の判断で。

### 3.8 ★ 緩めるのをやめた（2026-09-11・3 通り測って、3 通りとも使えなかった）

§3.7 の「`_looks_like_org` を緩めるなら 87 冊を測り直すのが先」を実行した。
**製品を触る前に**、拾い方の案を 3 通り、両方の束で測った。

| 案 | 既存 87 冊 | 入れ子 14 冊 |
|---|---|---|
| 連絡先ブロック全体（〒/TEL の ±6 行） | 答えは 80/87 で候補に入るが、`請求書`・`請求日：`・住所・`数量` も一緒に入る → **80 冊が候補 2 つ以上** | 12/14 だが同じ |
| `〒` の隣 2 行・数字を含む行を除く | 67/87 命中・14 冊で候補ゼロ・**40 冊が候補 2 つ以上** | 11/14・5 冊が 2 つ以上 |
| 登録番号（T+13）の隣 | **14 命中 / 13 外れ**（`請求書No`・`請求日` を拾う） | 1 命中 / 2 外れ |

★★ **測定の答えは「緩めるな」だった。** 法人格の無い名前は、位置や隣接では
見出し・住所と見分けられない。どの案も、除外語を足し続けない限り成り立たない ──
それはこの repo が繰り返し踏んだ「列挙で当てる」形そのもので、
列挙に無い言い方が来たら黙って落ちる。

★ 3 案目を測っている途中で、俺は `請求番号：` を除外語に足したくなっていた。
  **手が動いた方向そのものが答えだった** ── 調整を重ねて見た分に当てはめる側に入りかけていた。

### 3.9 代わりに直したこと（振る舞いは変えない）

拾えないなら、**拾えないと正直に言う**方に倒した。理由の文面を変えた:

```
前  発行元（請求元）の名前が見つかりませんでした
後  発行元（請求元）の名前が見つかりませんでした
    （『株式会社』などの法人格が付いた名前だけを探しています ──
      屋号や略称だけの請求書では見つかりません）
```

★ 「見つかりませんでした」だけだと、人は**探し方**を疑えない。
何を探したのかを言えば、「うちの取引先は屋号だ」と分かった時点で人が判断できる。

実測は動かない（87 冊 171/171・入れ子 18/25・誤取得 0）── 文面だけを変えたので当然だが、
**確かめずには書かない**。

★ 発火条件（拾いに行く側に戻る条件）: 屋号・略称の請求書が実需で来た時。
  その時は「位置で当てる」ではなく、**別種の根拠**（買い手が取引先名簿を持っている等）を探す。

## §4 束で疑う（2026-09-11・C）

### 4.1 なぜ束か

需要の上位 5 つのうち 4 つは複数ファイルで、売り物は「代わりに疑うこと」（§0）。
1 冊ずつは正常でも、同じ取引先が毎月出る束では **同じ請求書が 2 通・同じ月に金額の違う 2 通・
1 冊だけ年が違う・桁が違う** が見える。これは一覧の値を作る話ではなく、**人が見る場所を指さす**話。

### 4.2 先に凍結したこと（検体が来る前）

- 答えは**束（フォルダ）単位**。検体を書く側（規則を知らない・`form_read.py` 等を読ませない）が
  「仕込んだ疑い（種類・関わる冊・名指しすべき語）」と「**怪しくない冊**」を宣言する。
  怪しくない冊の宣言が無いと「何でも疑う道具」が満点になる。
- 見出しにする数は 正／**偽の疑い**（怪しくない冊を疑った・最悪）／取り逃し／不親切（冊か語が欠ける）。
  正答率にしない。
- 対照: 答えをそのまま返す→全部正・黙る→取り逃しだけ・全部疑う→偽の疑いが立つ。
  ★ 初版の採点器は「当たった疑いに怪しくない冊を巻き込む」形を無罪にしていた（全冊疑う変異で露見）→ 締めた。
- 変異: 金額を 1 円ずらす→重複が消えて訂正再発行になる／片方の冊を消す→重複が消える。

### 4.3 検体（5 束 51 冊・仕込み 8・怪しくない 36）

| 束 | 冊 | 仕込み |
|---|---|---|
| 通常 | 9 | 無し（陰性対照） |
| 8 月受領分 | 15 | 重複・訂正再発行（枝番あり）・訂正再発行（番号も日付も読めない骨） |
| 9 月受領分 | 9 | 請求日ラベルだけ空・年の誤り（2025） |
| 柏原グループ | 9 | 桁違い（×10）・番号がとぶ・番号が重なる |
| 桐生グループ | 9 | 無し（「単価だけ 1.6 倍」は書き手も迷い、どちらでも正解） |

書き手と別実装で番地の値を突き合わせた（608/608）。束の割り方（疑い／怪しくない／迷う）に穴なし。

### 4.4 実測（予測を凍結してから回した ── 予測どおり）

**器官（冊ごと 5 項目）**: 正 227 / 誤報 0 / 安全 28（分母 255）
- 安全 28 = 法人格なし 9（§3.4）＋ inv21 の骨 14（日付・番号のラベルが**セルでなく表示形式に焼き込まれた実物雛形**）＋ 令和 2 ＋ 繰越の骨 3

**束の疑い**: **正 5 / 取り逃し 3 / 偽の疑い 1 / 不親切 0**

| 仕込み | 結果 | なぜ |
|---|---|---|
| 重複 | 正 | 請求日・番号・金額が同じ |
| 訂正再発行（枝番） | 正 | 同じ月・金額違い・枝番 `501-2` を名指し |
| 年の誤り | 正 | 他の冊から 6 か月以上離れた年（12 月→1 月の普通のまたぎは 1 か月） |
| 桁違い | 正 | 他の月の中央値の約 6 倍かつ桁数が違う（閾 4 倍・検体の普通の変動は最大 2.3 倍） |
| 空欄 | 正 | 同じ取引先の他の月には請求日がある |
| 訂正再発行（inv21） | 取り逃し | 日付が読めないので「同じ月」が言えない |
| 番号が重なる（inv21） | 取り逃し | 番号が読めない |
| 番号がとぶ | 取り逃し | **意図して入れていない**（4.5） |
| 檜山 06 | ★偽の疑い | 令和の日付が読めず「空欄」に見えた |

陰性対照の束で偽の疑い 0。変異 2/2。

### 4.5 ★ 入れなかった疑いと、その理由

「番号がとぶ」── 取引先の請求番号は**その取引先の全顧客**で通しなので、買い手の手元に欠番があるのは普通。
鳴きすぎる見込みで入れない。検体では 1 件取り逃す。実需で「欠番を見たい」が来たら測ってから。

### 4.6 設計の線（`ailine_core/forms_suspect.py`）

- 入力は「冊 → 項目 → 値」。値は `field_record.value()` が出したものだけ（割/無 は None）。
  **None は根拠にしない**（読めなかったものを疑いの材料にしない ── 空欄は誤値より安い、はここでも同じ）
- 取引先が読めない冊は取引先ごとの照合に入れない（§3.4 の穴の値段が束でも見える: 9 冊）
- 値は作らない。所見は『束の所見』シートと `--json` の `suspicions` に出す。報告には件数と種類だけ
- 種類の語は `forms_suspect` にしか書かない

### 4.7 次に測ってから決めること

1. ~~令和を西暦に直す~~ → **§5 で済んだ**
2. ~~inv21 の表示形式に焼き込まれたラベル~~ → **§6 で済んだ**
3. 桁違いの閾 4 倍は検体の変動（≤2.3 倍）から置いた。実物の月次変動は未測 ── 鳴きすぎたら閾でなく「約 N 倍」の表示で人が判断できる形は保ってある

## §5 和暦を読む（2026-09-12）

### 5.1 先に測った ── 証拠の出所を正直に置く

| 群 | 和暦の日付が在るセル | うち請求日ラベルの右 | ★ 元号だが日付でない |
|---|---|---|---|
| 検体 v2 87 冊 | 0 | 0 | 0 |
| 入れ子 14 冊 | 0 | 0 | 0 |
| 束 51 冊 | 7（2 冊） | 7 | 0 |
| 手つかずの実物 26 冊 | 0 | 0 | 0 |

★ **実物の 0 は「効かなかった」ではなく「測れなかった」** ── 実物 26 冊は請求日ラベルが
19 あるのに、**日付が 1 つも入っていない**（雛形だから・西暦も 0・日付型も 0）。
★ そして束の 2 冊は、**俺が検体の依頼文に「令和表記を 1〜2 冊」と書いたから在る**。
つまり和暦についての証拠は、実物からは何も出ていない。それでも足した理由が次。

### 5.2 §3.8 で緩めるのをやめたのに、ここでは足した ── 線はどこか

| | 法人格なしの社名（§3.8・却下） | 和暦（採用） |
|---|---|---|
| 足すもの | 除外語・位置の規則 | 元号の一覧 |
| その一覧は | **無限に増える**（言い回しは尽きない） | **法で決まって閉じている**（代替わりの時だけ増える） |
| 当たり方 | 位置や隣接からの推測 | 暦の**算術**（令和8年 = 2018+8） |
| 外れた時 | 住所や見出しを名前として出す | 日付の形に合わなければ **None**（値を作らない） |

★ 判別の問い: **その一覧は足し続けないと当たらないか。** 足し続ける形なら列挙で、
列挙に無い言い方が来た日に黙って落ちる（この repo が何度も踏んだ形）。
★ 実測でも裏づけた: 元号が**日付でない文脈**に出たセルは 178 冊で **0**。

★ 足さなかったもの: `R8.8.31`・`H31/4/1` のアルファベット略記（178 冊で 0 件・
`R8` は品番にも部屋番号にもセル番地にも見える）。**発火条件つきで保留**し、
実物に出たらその時に測って足す。

### 5.3 置き場所 ── 暦の算術は 1 箇所、どの暦を受けるかは呼び出し側

```
date_compare.parse_wareki_literal   暦の算術（判定）★ ここだけが元号を知る
form_read.read_issue_date           d = _parse_date(raw) or _parse_wareki(raw)   ← 合成
```

★ `parse_date_literal` には**混ぜない**。あちらは依頼文の値を読み、
`classify_date_column`（列の型判定）にも使われる ── 混ぜると `ailine run` の経路と
列の型判定へ**測っていない範囲まで黙って波及する**。測ったのは帳票の側だけ。
★ この「広げていない」を番人にした（`tests/test_wareki_is_read_only_by_the_form_organ.py`）──
2 つを 1 つに畳もうとすると赤くなる。変異 3 通り（合成を外す／依頼文へ広げる／元号を 1 年ずらす）
すべてで赤。

### 5.4 実測（予測を凍結してから）

| | 予測 | 実測 |
|---|---|---|
| 束・請求日 | 安全 9 → 7 | ○ 正 42 → 44・安全 7 |
| 束・偽の疑い | 1 → 0 | ○ 0（正 5・取り逃し 3 は不変） |
| 87 冊 | 変化なし | ○ 171/171 |
| 入れ子 | 変化なし | ○ 18/25 |
| `run` 経路 | 変化なし | ○（`parse_date_literal` を触っていない） |

### 5.5 ★ 予測が外したもの ── 物差しの穴

予測に無い赤が出た: **請求日の誤報 0 → 2**。中身は
`出した '2026-8-31' / 正 '令和8年8月31日'` ── **製品は正しく、採点器が和暦を知らなかった**。
答えはセルの文字そのものを持つので、物差し側の正規化が和暦を素通りさせていた。

★ 予測は**製品について**立てていて、**測定器は予測の外に置いていた**。
もしこの 2 件を製品の欠陥として追いかけていたら、正しい実装を「直して」壊していた。
★ 直したのは物差しの側だが、**製品の関数は呼ばない**まま直した
（暦の実装を採点器にも別に持たせた ── 同じ関数で作った分母は分母でなく感想）。


## §6 ラベルが表示形式に焼き込まれた帳票（2026-09-12）

### 6.1 何が起きていたか

画面には「請求日： 2026年6月30日」と見えるのに、**『請求日』という文字がどのセルにも無い**。
ラベルは表示形式（number_format）の中に在り、セルの中身は日付型だけ:

```
H6   値 = 2026-06-30（日付型）   書式 = "発行日： "yyyy\年m\月d\日;@
H19  値 = 20260601（裸の数値）   書式 = "請求書番号： "0_);[RED]\(0\)
H20  値 = 2026-06-30（日付型）   書式 = "請求日： "yyyy\年m\月d\日;@
```

セルの文字を読む器官には、ラベルが 1 文字も存在しない ── だから請求日も請求番号も 無 だった。

### 6.2 先に測った ── 珍しい癖ではなかった

| 群 | 焼き込みラベルのセル | 冊 | 値の型 |
|---|---|---|---|
| 検体 v2 87 冊 | 日付 26・番号 13 | **13 冊** | 日付型 26・数値 13 |
| 入れ子 14 冊 | 日付 4・番号 2 | 2 冊 | 同上 |
| 束 51 冊 | 日付 14・番号 7 | 7 冊 | 同上 |
| 手つかずの実物 28 冊 | 日付 2・番号 1 | 1 冊（inv21） | 同上 |

★ 形は **3 種類だけ**で、値の型は例外なく 日付型／数値。
★ 拾いすぎの危険の分母も測った: ラベル語を含む表示形式は、全 180 冊で
  日付型／数値のセルにしか付いていなかった（`円` を焼き込んだ書式は 506 件あるが別語）。
★ **87 冊のうち 13 冊**がこの形だったのに今まで見えていなかったのは、
  v2 の答えに請求日・請求番号が無いから ── **測っていない所は、壊れていても点数に出ない**。

### 6.3 直し

- `form_grid.Cell` が表示形式を運ぶ（結合セルは**アンカーの書式**）── 器官の層は意味を持たない
- `form_read.format_label(fmt)` が引用部分だけを取り出す（書式コードは落とす）
- 請求日・請求番号の読み手が、ラベルの右／同居 に**焼き込み**を足して合成する
- 請求番号は**識別子であって数ではない**ので文字で出す（`20260601.0` に化けないよう均す）

★ 同じ冊に 発行日 と 請求日 が両方在り、**同じ日**を持つ（手つかずの inv21 も）。
  2 つの口が一致 → 値は 1 つ。食い違えば 割（空欄＋理由）── 既存の機構のまま。

### 6.4 実測（予測を凍結してから ── 全部当たった）

| | 予測 | 実測 |
|---|---|---|
| 束・請求日 | 正 44 → 51・安全 0 | ○ |
| 束・請求番号 | 正 44 → 51・安全 0 | ○ |
| 束の疑い | 取り逃し 3 → 1（残るのは「番号がとぶ」だけ） | ○ 正 5 → **7**・偽の疑い **0** |
| 87 冊 | 171/171 のまま | ○ |
| 入れ子 | 18/25 のまま | ○ |
| 87 冊の区分 | 請求日 無 86 → 無 73・単 13（割 1 は不変） | ○ |

変異 4 通り（器官が書式を運ばない／焼き込みを読まない／裸の数値を均さない／
ラベルを見ずに何でも拾う）すべてで赤。

### 6.5 残ったもの

**「番号がとぶ」1 件だけ**が取り逃し ── これは §4.5 で**意図して入れていない**
（取引先の請求番号は全顧客で通しなので、買い手の手元の欠番は普通）。
つまり束の検体で、道具が拾えるはずのものは**全部拾えた**。


## §7 PDF を測って、Excel 側の欠陥が出た（2026-09-12）

### 7.1 なぜ PDF を測ったか

実務で受け取る請求書は PDF が圧倒的に多い（Namakoo の判断）。ailine が読むのは xlsx なので、
射程がどれだけ狭いかを確かめる必要があった。

★ 構造は有利だった: `form_grid` `form_read` `field_record` `forms_collect` `forms_suspect`
`date_compare` の **1872 行のうち、Excel を知っているのは 13 行**。
残りは `Grid` の 8 つの口（`right_of` / `below_of` / `text_cells` …）の上に載っている。

### 7.2 実物の日本語 PDF で分かった罠 2 つ（どちらも静かに全滅する）

1. **`pdftotext`（poppler）は日本語を 1 文字も返さない。** 数字と記号だけ返すので
   「空でない＝成功」に見える。`pdfplumber`(pdfminer) は返す
2. **漢字が康熙部首に化けている。** `請求⽇` の「⽇」は **U+2F47** で、`請求日`(U+65E5) と
   別の文字。Adobe の日本語雛形 16 枚すべてでこれ。見た目は完全に同じなので目でも diff でも
   気づけず、**ラベル照合が全部外れるのにエラーは 1 件も出ない**。→ 取ったら**何より先に NFKC**

### 7.3 実物 4 レイアウト（記入例）── 器官は無改造

PDF → `Grid` の変換器 80 行だけを書いて、`form_read` は 1 行も触らずにかけた:

```
記入例 4 冊 × 5 項目 = 20 のうち 取れた 13 ／ 誤報 0 ／ 空の雛形 4 枚から値を作った 0
```

★ **Adobe の公開サンプルの誤りを捕まえた**: 雛形 4 の記入例は
「請求金額 330,000円」と書いてあるのに明細の合計は 5,440 円（雛形 1 の例文の数字が残っている）。
器官は値を作らず、両方の番地を名指しして空欄にした。

### 7.4 ★ 87 冊を PDF 化して測ったら、予測を大きく外した

| | 予測 | 実測 |
|---|---|---|
| 正 | 130 ± 20 | **53**（31.0%） |
| 誤報 | 0 | **14**（8.2%） |

★ 外した理由: Adobe の 4 枚（13/20）から外挿した。あれは単純な 1 段組で、
87 冊の骨は実物ベンダーの複雑な版面（結合・多段）。**Excel は行・列・結合をタダでくれるが、
PDF ではそれを復元する側が要る。80 行の復元は素朴すぎた。**

### 7.5 ★★ 誤報 14 件は全部同じ形 ── 買い手を請求元として出していた

```
B19 請求元: 出した 'ナギ商会株式会社'（買い手） / 正 '寺岡製作所有限会社'
…14 件すべて同じ
```

原因の鎖:

```
格子の再現が甘い → 宛先が決まらない
  → read_issuer の「宛先の名前を含む候補を除く」の taken が空になる
    → 除外が黙って無効化され、買い手の名前が唯一の候補として残る
      → 候補 1 つ ＝ 単 ＝ 自信を持って間違える
```

★ そして `read_issuer` の docstring は**以前からこう宣言していた**:

> 残る T11 は宛先そのものが 割 なので買い手を除けない ── **そこは決められないのが正直**で、
> 検体も期待を宣言していない

**宣言はそう書いてあるのに、実体は値を出していた**（依頼/宣言/実体の三項のうち、宣言と実体の食い違い）。

★ **これは Excel 側の欠陥でもあった。** 宛先が一度も失敗しなかったから発火しなかっただけ
（出ないことは信号でない）。番人は「宛先が欠けたときの請求元」を一度も試していない（負の被覆）。

### 7.6 直し ── 宛先が決まらない冊では請求元を決めない

処置の値段を先に測った:

| 群 | 宛先が決まらないのに請求元を出している冊 |
|---|---|
| 87 冊 | 0 |
| 束 51 冊 | 0 |
| 入れ子 14 冊 | **1**（入09・BPO）── その冊は**そもそも拒否が正解**（期待は 割/無） |

実測（処置後）:

| | 前 | 後 |
|---|---|---|
| 87 冊 | 171/171 | 171/171（不変） |
| 束 51 冊 | 243/255 | 243/255（不変） |
| 入れ子 | 誤区分 1 | **誤区分 0**・不親切 2 |
| PDF 87 冊 | 誤報 14 | **誤報 3**（請求元は 11 → **0**） |

★ 予測になかったコスト: PDF で正しく読めていた請求元 11 → 2 ── **9 冊は正解を失った**。
誤値 11 を消して空欄 9 を増やした取引で、この製品の価値観（空欄は誤値より安い）では得。
★ 残る PDF の誤報 3 はすべて請求額で、**格子の忠実さ**の問題（Excel では 0）── §7.7 の宿題。

### 7.7 次（PDF を本流にするなら）

★ 訂正: 「PDF 対応は `Grid` の第二の作り方を足すだけ」は構造としては正しいが、
**規模の見積もりが甘かった**。格子を作る所が製品の正直さを全部背負っている ──
今日それが実証された（甘い格子 → 自信のある誤値）。

1. 格子の復元を Excel 側と同じ厳しさで作る（誤報 0・陽性/陰性対照・変異試験）
   ── 結合セルに相当する「1 つの文字列」の復元、列の割り当て、明細表の切り出し
2. 本物の受領 PDF（会計ソフト発行分）で当てる。今日測ったのは 1 社の 4 レイアウトだけ
3. スキャンした紙は別問題（テキスト層が無い ── OCR の射程）
