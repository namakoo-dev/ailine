"""op ごとの**事後条件**（書いた直後に読み戻して確かめる層）。

★ 分割の方針（2026-09-03）: 29 op の事後条件を、**表に何をする op か**で 3 群に割る。

    shape   … 行数も列数も変わらない（値と見た目）        12 op
    move    … 行や列の数・位置が変わる                   10 op
    derive  … 新しいシートや別ファイルを作る               7 op
    _shared … 2 群以上から呼ばれる読み方と開示の言い方

★ 群の割り当ては人が決めたが、**過不足が無いことは機械が検算している**
（`tests/test_postcondition_ledger.py` が `OP_LABELS` との突き合わせを持つ）。

★ この層はブックを**読む**（`BookView` / `Path.exists`）。純ロジックではない ──
「書いた結果を読み戻して確かめる」のがこの層の役割そのもので、
読まずに済ませたら事後条件ではなくなる。
"""
from __future__ import annotations
